---
name: project_wa_read
description: "WaRead — read-only WhatsApp group reader (linked device, Baileys 6.7.24) at E:\\Dev\\scripts\\WaRead; /whatsapp skill; link state; why the desktop-app path is banned"
metadata: 
  node_type: memory
  type: project
  originSessionId: 73387deb-cd38-48d9-99c3-6231a2e51b8d
  modified: 2026-09-10T11:28:09.864Z
---

**WaRead** (built 2026-09-10) = the ONLY path for reading みや's WhatsApp. Repo `E:\Dev\scripts\WaRead` (local git, no remote yet). `node E:\Dev\scripts\WaRead\wa-read.js read "<group>" --limit 20` · `groups` · `sync` · `status` · `link --phone 60…` · `unlink` · `_selftest.js` (32 checks). Skill = [[whatsapp]] (`.claude/skills/whatsapp/SKILL.md`, eval `domain/whatsapp/eval.js`, 20 checks).

How it is read-only: `lib/readonly.js` hands the program an ALLOWLIST view of the Baileys socket (ev · groupFetchAllParticipating · groupMetadata · fetchMessageHistory · requestPairingCode · end) and poisons every other method on the real socket (sendMessage, readMessages, sendPresenceUpdate, chatModify, logout, group*, …) so they throw. `markOnlineOnConnect:false`. Library still sends delivery acks + an "unavailable" presence by itself (invisible as activity). Baileys = unofficial client → small account-flag risk, keep to reading.

**LINKED 2026-09-10 18:56** as `60186669566:42@s.whatsapp.net` (fixed pairing code `WAREAD26` via `--code`; windows rotate every ~3.5 min because WhatsApp drops an unpaired socket, 428 then 401 on a reused identity → fresh identity per window). Baileys **7.0.0-rc14** (6.7.24 pairing failed). 143 groups known; pinned flag did NOT arrive in the first history sync; chat order = `conversationTimestamp`.

**Watcher design (miya 2026-09-10 evening, NOT BUILT yet)**: goal = detect work messages and auto-start a Claude Code session that runs an ADHOC /quest to the brief and STOPS (Opus 4.8, xhigh effort). Two filters: (1) script, deliberately loose; (2) Ruri judges before any session starts. Groups: **#1 Release Melaka Pelupusan ⏰** · **MLK Pelupusan Support** (front line, BA↔users/TSO) only when a ticket number / "ticket created" / "dah create tiket" / his name or tag appears · **PERAK PLP** only when his name is mentioned/tagged · **Alex** (manager) in any group or DM: questions about tickets → session with that context; tickets passed → session that retrieves the ticket, confirms Melaka or Perak, runs the quest to brief (issue · solution · story diagram) and stops. Message "shape" = the adhoc-paste-detector shape (Urusan:/Tugasan:/Id:/User: labels, or permohonan-id + issue words). Session name format: `<MLK|PRK><ticket> - <Urusan if any> - <very short keyword>`. Casual groups are never read (token discipline). Read-only guarantee holds for sends/read-marks (allowlist wall); library still emits delivery acks + "unavailable" presence.

**Research + live test 2026-09-13**: (1) 🚨 `-p` sessions CANNOT be opened by Remote Control → children must be spawned as `claude --remote-control "<name>" --session-id <uuid> …` (interactive process, phone-reachable); judge stays `-p --resume`; needs a claude.ai login not an API key; `-p` sessions are absent from the `--resume` picker (resume by id only). (2) 🚨 the standalone `claude` CLI login on this laptop is EXPIRED (401) → he must run `claude login` once before any daemon test (MAP WA-12). (3) Media download is a pure read via the module export `downloadMediaMessage` with NO `reuploadRequest`; must happen at receipt time (store drops the locators); CDN links expire. (4) 🚨 OFFLINE-DELIVERY GAP: two syncs (30 s, 90 s) got 0 new messages and the store holds NOTHING after 2026-09-11 15:07 across all chats → either WhatsApp does not replay offline messages to this companion or the reader misses them; "catch-up from cache" is UNPROVEN (MAP WA-11) — the daemon may have to hold the socket 24/7. (5) Live test of "read Alex's DM": the store has NO contact names (phone never pushed the address book) and no sender display name contains "Alex"; he declined to type the number and chose the guess DM (5 unread, 09-10 18:21, 1,729 rows) — its content was a friend asking for an opinion, no work issue → judge verdict SKIP, no session started. Identity of Alex's chat still unknown; DM reading by contact is WA-04.

**Final architecture (2026-09-13, his "both benefits" ask)**: always-on WaRead daemon (Node, logon start) → loose script filter → **judge = ONE headless Sonnet session per day, resumed by the daemon for every candidate** (`claude -p --resume <deskId>`), so it keeps the day's context without Claude Code being open → children = headless named sessions (`claude -p --name "<MLK|PRK><ticket> - <Urusan> - <kw>" --session-id <uuid> --model opus-4.8 --effort xhigh --permission-mode dontAsk --allowedTools …`), follow-ups resumed into the same child · permission mode `dontAsk` + allowlist, must be AUDITED end-to-end (he stressed severity: the child must create the quest md + scaffold without a single prompt) · phone access via `claude remote-control` = research ticket · the map with 10 tickets = `domain/whatsapp/MAP.md` (main checkout). He does not know the word "headless" (= claude run by a script, no window, his normal login); do not ask billing-type questions again.

**🚨 THE PURPOSE (miya 2026-09-13, verbatim intent, never drop it)**: a Claude Code session STARTS BY ITSELF the moment a work message arrives on WhatsApp, even when he has not reached the office or the laptop yet, with the photos/videos already downloaded and the described issue carried in, so he can answer fast and accurately without downloading or copy-pasting. It runs always, stays aware, and sessions start at the time they are requested (late desk start = process the backlog since last close, oldest first). He does NOT want Claude Code bloated and does NOT want to type a command or open the app to make it work. Slip 2026-09-13 `purpose-drift`: I recommended a desk session that needed the app open + a typed command; he was furious ("I always told you to remember the purpose"). The persistent part must live OUTSIDE Claude Code sessions (the WaRead daemon), which spawns sessions itself. He also asked: save/document the daemon + setup completely under `domain/whatsapp/` so it can be brought to another laptop.

**Watcher map decisions, rounds 5-6 (2026-09-12, popup)**: **DESK + children architecture (his idea)** = one day-long DESK session holds the day's context, judges each candidate (new issue / follow-up / baseline / skip), SPAWNS one named child session per issue, and SENDS follow-ups into the existing child via session-to-session messaging (`mcp__ccd_session_mgmt__send_message`, verified present; in-session only, unavailable in unattended runs) · feed = **Monitor tool** on a WaRead stream (event-driven, zero tokens between events), NOT /loop · WaRead daemon runs always-on at Windows logon so the cache never misses; the desk catches up from the cache when started late · rules home = `domain/whatsapp/RULES.md` (routing table: message shape → meaning → skill (/adhoc scaffold · /quest resume · /release-mlk-plp baseline on Mon/Wed, later add/remove tickets = message into the Baseline child) → spawn / send-into / skip); wayfinder map = `domain/whatsapp/MAP.md` · adhoc week file = `1. Tasks\Melaka\adhoc-week.txt`, REGENERATED from active.txt ADHOC blocks by script (system-rules Rule 6 class `regenerate`), never hand-edited · env label (Prod/Internal) judged from context (forwarded-from-TSO shape, PDT* structured message, URL host, words), `?` when unsure · media grouping = per ISSUE by the desk (replies, captions, sender, topic), not by time window.

**Watcher map decisions, rounds 3-4 (miya 2026-09-11 16:30 + 2026-09-12, popup)**: run window when live = ALWAYS ON · name match = real @mention + whole-word spellings `Ahmad`, `Ridhwan`, `@Ridhwan`, `@AhmadRidhwanAnuar`, `@Ahmad` · extra trigger words `Hi dev` + office codes `PDTJ / PDTAG / PDTMT` count in **Release Melaka Pelupusan only** · **NO pop-ups at all** (he always reads WhatsApp + always has Claude Code open; the value is the session STARTING BY ITSELF with the photos/videos already downloaded and the described issue carried in, so he answers fast without downloading or copy-pasting) · dry-run row = room · time · rule hit · judge verdict + reason · session name it would open (no text, no names) · end-of-day list = a daily TEXT FILE for keying Protime "Adhoc": one file overwritten once per week, days divided by `====<date>===`, rows `1) PRK - Prod/Internal - <very short issue>` · OPEN: media/issue grouping when conversations overlap (his worry: same-sender window could miss a second issue or attach the wrong photo) · file location for the adhoc list. Reader gaps found: store keeps no media bytes (text.js renders `[image] caption` only; `downloadMediaMessage` not wired; `updateMediaMessage`/`refreshMediaConn` are on the DENY list), and rows carry no quoted-message / mention ids.

**Watcher map decisions (wayfinder rounds 1-2, miya 2026-09-11 ~16:05, all via popup)**: destination = DRY RUN over the cached last 7 days of the 3 work rooms printing a "would have fired" list + the rules doc (live auto-start = the NEXT map) · map file = `domain/whatsapp/MAP.md` (local Markdown, no tracker) · Alex scope = the 3 work rooms + his DM (CLI reads groups only today, DMs already stored → small reader change) · spawned-session model = Opus 4.8 xhigh · session start = a NAMED desktop-app session `<MLK|PRK><ticket> - <Urusan> - <keyword>` (spawn_task path), not headless · judge = a small separate Sonnet call (fire / skip / hold + one-line reason) before any spawn · already-tracked ticket = NO second session, toast + append the paraphrased message as a dated note to that quest doc · notification = Windows toast at fire time + one end-of-day digest (fired / held / skipped). Facts checked: cache holds 7 days for all 3 rooms (73 / 139 / 343 rows), `watch add/remove` exists.

Link state (history): NOT LINKED as of 2026-09-10 afternoon — needs his phone number (country code, digits) for the pairing-code flow (`link --phone` prints XXXX-XXXX; he types it under Linked devices → Link with phone number instead; device shows as "Chrome (Ubuntu)"). Auth lives in `%USERPROFILE%\.wa-read\auth` (never in a repo); message cache `%USERPROFILE%\.wa-read\messages.jsonl`. History: the phone pushes recent history on link; `read` calls `fetchMessageHistory` for older rows when the store is thinner than `--limit`.

**Why:** 2026-09-10 the computer-use path on the WhatsApp desktop app needed a grant each session, marked a group read when opened, and the app stopped taking clicks/keys mid-task (DBeaver kept jumping to front); the Tech Team group was never read that day. Desktop app / WhatsApp Web / Chrome are BANNED as reading paths now (see [[feedback_whatsapp_read_rules]]).
**How to apply:** any "read/check WhatsApp" ask → invoke the whatsapp skill → run wa-read.js; if not linked, ask for the number first. Minimize the desktop app if it was ever brought forward.
