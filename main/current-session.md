# Current Session

## 2026-09-09 (S1, ~10:00–14:30) — PymTime day: distribution-ready · Drive script v4 (no second file) · first-run safety audit M1–M26 · toast/tab icon saga

**Arc** (repo `E:\Dev\scripts\PymTime`, private GitHub, NOT this repo; last commit `38b1780`, zip 33.03 MB rebuilt): day-off cell shape settled (label "Next holiday", date bold, leave name small). Buttons two-column layout built then reverted on request. Beta pill removed; How it works split Daily / Monthly; readmes rewritten; personal strings swept out of the bundle; file-name token from Protime now remembered server-side. miya's question "does upload detect my hand-uploaded file under a different name?" → honest answer NO (S10 even asserted the duplicate) → **Apps Script v4 deployed 12:33** (`exists-similar` on a hand-uploaded look-alike by name token ≥3 letters or login; `already-ticked` when the Checklist box is ticked and nothing of theirs is recognisable; force only from code, page never offers it) → verified live: July upload named Ridhwan refused, folder unchanged (live scenario S36). First-run safety audit written as M1–M26 in `FAILURE-MODES-AND-TESTS.md` with proof per row (Windows never fires a task whose start time passed today — probed; dry run on a clocked-in account → already-clocked). Self-test 459/459, live scenarios 36/36.

**Icon saga (4 corrections from miya)**: (1) added a big appLogoOverride image instead of fixing the small header icon → reverted; (2) rebuilt the SHARED ico → desktop shortcut + browser tab changed as collateral → ico files restored byte-for-byte, slip logged `collateral-change`; (3) Windows notification platform caches the FIRST IconUri path per app (re-pointed path = blank icon; proven by restoring the old path with new art) → toast icon is now its own PNG at a fixed path `PymTime-toast.png` (transparent, ring 28 wide; every other app on the PC registers a PNG); (4) tab icon = the same PNG, served at `/icon.png` (fresh address beats the browser favicon cache; hard refresh confirmed). Toast text "Already clocked in — You clocked in yourself at 08:41." (test toasts carried the test time, real runs carry Protime's clock-in time).

**Resume**: nothing open on PymTime. Colleague hand-out = `E:\Dev\scripts\PymTime\PymTime-portable.zip`. Only uncovered upload case: a hand file sharing no letters with the Protime name or login AND an unticked Checklist box. MemoryCore: etanah quests untouched today; ADHOC-REDMINE-RC-2026-1 deadline was yesterday (2026-09-08) — unaddressed; 2 new Redmine tickets #278930 / #278909 (2026-09-09) have no local block yet.

## 2026-09-08 (S1, ~17:40–22:10) — Perak PLMS #278218 "hotfix failed": deploy-miss PROVEN · harden pushed · recurring-mistake audit → Perak guards G1–G5 · branch-push cleanup

**Arc**: continued from compaction on worktree `start-quest-check-888f34` (repo checkout is on `main`). TSO reported the #278218 fix "failed" on PROD hotfix. A parallel session had already banked the full proof in `QA-278218.md` (P1–P8): the hotfix host `192.168.18.110` ran a **master** build (footer `Branch Name: master`, Build 08/09 10:50:37), `git master` lacks `b86ab0fbd5`, and PROD error store `ET_SISTEM.PT_APPLICATION_EX_ENTITY` rows 3816554/3816726 name that build + the `tarikBalik` exception → **deploy-miss, not code failure**. Fix proven on staging (P7). DB discriminator: keputusan stored under **PKMMKN** (497 apps) not just KKMMKN (496) — the code read only KKMMKN → empty → variable never emitted. miya ruled "commit new code": wrote + pushed harden `7a1e8d4786` on `prk/esokongan/278218` (read PKMMKN keputusan first, fallback KKMMKN, always emit `tarikBalik`), compile green, extends `b86ab0fbd5`. Then git cleanup: pushed the only genuinely-unpushed branch `prk/esokongan/277115` (GUI ↑ badges were vs master, not origin — everything else already synced).

**Corrections from miya (all one root — anchoring to the conversational turn over banked verified evidence)**: (1) retracted the PROVEN deploy-miss to "unknown/can't access" when told "trust infra" — the host IS reachable + the error store proves it; (2) proposed "alter forward to next tugasan" 3× after he said each time: insert the missing variable in place, not forward; (3) treated the ticket's original Keputusan-MMK photo as "new evidence"; (4) nearly edited on the wrong branch (`prk/esokongan/277115`, not 278218) — caught by a git-state check before the edit.

**Banked**: `perak/index.md` §Perak quest-workflow guards G1 (ground-truth anchor = qa_doc verified rows outrank push-back) · G2 (deploy-miss is first suspect; prove via footer + `git branch -r --contains` + `PT_APPLICATION_EX_ENTITY`) · G3 (branch-check before edit) · G4 (one correction = stop re-proposing) · G5 (missing flowable var = code+deploy, not utility-page alter). `perak/BUG-BESTIARY.md` PLMS tarikBalik/PKMMKN pattern. `perak/BRANCH-AND-DEPLOY.md` §3/§4.1 already carried the deploy-verify method. Fixed `domain/checklist-reactivate/resume-readiness.js` PERMOHONAN_RE `PT[A-Z]{3}` → `{2,4}` (was false-negative for Perak `PTPK` = PT+2 letters). Slips: verify-before-claim, phase0-evidence-read, ground-truth-anchor, rejected-approach-repeat.

**Resume**: QA-278218 = ✅ cold-resume ready. PENDING = Infra rebuild from `prk/esokongan/278218` (or `release/1.74.0`), NOT master → azurarahman Hantar on PKMMKN `21809341` → Ruri verifies `UMM_A_TGSN` progression. QA-278109 closed (Ammar data patch). ⚠️ this `main` checkout is 7 behind origin/main with a large uncommitted multi-session backlog (pymtime, arabic, states.json, settings.json, melaka knowledge…) — surfaced in Handoff, NOT auto-committed.

## 2026-09-07 (S1, 16:29–17:55) — #278585 PT upload >1 MB: quest start → 1-line fix → commit/int-env → miya PASSED on Redmine · Phase 1 CLOSED · Solution row added to hand-back

**Arc**: `/quest start 278585` (ESOKONGAN, PDTJ sitihanum, PT `PTMLK/02/L/PT/2026/6` apl 3404515, Penyediaan Risalat MMKN - PDT). BA Nurhafizah: >1 MB file does not appear after upload. Scout in one pass: `mlkUlasanJabatanTeknikalDataTable.xhtml:121` hard-codes `sizeLimit="1000000"`; composite default = `WebUtil.getFileUploadSizeLimit()` 1 GB (`fileUpload.xhtml:77`); PDF sizes lined up exactly (JKR 880,316 shown · MPJ 1,141,931 + Pertanian 1,539,243 absent); PROD `umm_a_dok_kmskn` had exactly 1 JT row; Kedah (chanjun `0576fc1ab6` 2026-09-04) fixed the identical shape 3 days earlier. miya: "straightforward, commit and push to deploy" → `mlk/esokongan/278585 @ 9a2d49f8f7` → compile-gate green → cherry-pick `mlk/int-env @ ce30128a07` → deploy card → miya deployed, tested on mlit, **PASSED in Redmine**. Phase 1 closed 17:5x; Phase 2 archive pending.

**Corrections from miya**: (1) "add a Solution row next to Root cause" → built into quest SKILL.md hand-back (2-col table) + `rootcause-format` hook extended (eval 18/18) + memory; (2) "10000 TB file accepted now?" → answered with the 3-layer limit diagram (browser 1 GB default · Undertow max-post-size · DMS); he ruled **keep the sibling default, bank the knowledge** → FLOW-TRACES.md §Upload SIZE limits + index row; (3) 🚨 test scenario named `PTMLK/01/L/PLPS/2026/21` with **0 JT rows** (panel empty) — "are you still a dumb fuck…" → slip `test-data-not-from-live-state`, picking query now counts `umm_a_jabatan_teknikal` rows; corrected app `PTMLK/03/L/PT/2026/16` @ anizah (3 rows).

**Gate frictions**: `redmine-write-gate` false-positive on read-only `ticket-load-verify.js` + `lib/states.js resolve` (proposal logged) · commit-approval hook parsed **QA-276549** from my reply (stray flag `.claude/state/commit-approved-QA-276549.flag`, attachment-ledger then demanded 276549's files) · `compile-check.js run pelupusan` wants the full module name `etanah-pelupusan`.

**Resume**: `close` QA-278585 → Phase 2 archive + bounty. Ticket still needs the Redmine planned-release list. #278580 (A28 twin) Task folder 187 created by the sync, untouched.
