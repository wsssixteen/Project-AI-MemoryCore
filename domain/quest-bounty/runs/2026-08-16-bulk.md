# Bulk bounty run — 2026-08-16 (weekend audit)

Trigger: みや — "close Phase 2 of the tickets… make it meaningful… truly reflecting the bounty part". Rule: **Redmine Closed ⇒ Phase 2 runs**. 16 candidates probed via Redmine API; 14 Closed-eligible; 11 harvested this run (1 familiar each, Sonnet 5, schema-forced; controller spot-verified cites incl. `BpmCallbackService.java:783`).

Knowledge landed: `BUG-BESTIARY.md` +10 entries · `FLOWABLE-KNOWLEDGE.md` +3 · `TEST-PERMOHONAN-INDEX.md` +4 (all §Bulk-bounty 2026-08-16).

## Per-quest verdicts + post-mortems (5-line each; ARCHIVE_STATE reconciliation inline)

### QA-273461 — PLPS No Lesen premature allocation
Symptom: OPLPS printed A01/2026/4 instead of /2; numbers missing from carian.
Root: `MlkPengiraanBayaranLesenForm.java:647-648` unconditional allocation at early tugasan (shared 4-urusan sequence).
Fix: additive urusan guard mirroring in-file `:576-580`; live `mlk/master @377580ef71`.
Slowed by: fix split across v1/v2/v3 branches, 2 reverted — no branch held the whole diff; content-hash diff vs `3b745e987f` was the proof.
1-step catch: diff release content against the known-complete fix, never trust "branch merged".
⚠️ Open residue: PROD `patch-273461.sql` (Redmine attachment) unrun as of 08-10 — **carried to standing-residue list**.

### QA-271985 — MLPS migrated-data blanks (4-issue BA restructure)
Symptom: blank Tujuan/fees/RM null/dash names on migrated MLPS apps.
Root: 4 readers pointed at native-shape columns; migrated rows use the tujuan_permohonan* pair.
Fix: 5 additive isMelaka()-gated fallbacks; Phase 1 closed `ac60993732` (doc's early sections predate the close — internal contradiction noted, close stands).
Slowed by: pre-DB Rubric produced an INVERTED candidate that all had to be discarded.
1-step catch: run the live-DB queries BEFORE writing any Rubric candidate.

### QA-270900 — BPRZ Peraku doc + SSMW peranan
Symptom: Peraku rebirthed BARU doc; SSMW showed only KPT, pick discarded.
Root: SEDIA missing from Peraku fetch list (`DokumenKeluaranService.java:327-330`) + `ind_tgsn.peranan='KPT'` override at `BpmCallbackService:783`.
Fix: extend URS_BPRZ re-fetch analog (+2 lines); peranan fixed via Kemaskini Tugasan UI (NULL rule respected).
Slowed by: built-then-reverted fix at submit-hook when defect is open-time — lifecycle-timing miss.
1-step catch: confirm the lifecycle trigger point (open/load/submit) before building.

### QA-273455 — PT/PSBS Maklumat Tanah blanks (counter path)
Symptom: Awam-filled fields blank on officer screen for counter-paid PT.
Root: officer AppHakmilik pre-dates workflow → copy gate false (`PelupusanSpocService:235`).
Fix: read-side fallback (6× in-file null-check idiom), widened PT→PT+PSBS; self-heal proven.
Slowed by: BUILD SUCCESS on base branch ≠ compiles on merge target (int-env collision).
1-step catch: compile the actual merge commit on the target env branch.

### QA-274182 — stale pelan on 4Ae/L1e (PROD)
Symptom: old Plot 1 pelan rendered; 4 stale rows in Carian.
Root: container MAX(createdDate) + write-path container-per-save bug.
Fix: delete-only PROD patch per みや; code fix + write-path deferred to own ticket.
Slowed by: volatile id_dok changed mid-quest (live app) forcing script re-key to a_dok_keluaran_id.
1-step catch: key patches on stable PKs from draft 1; cite JSF action= before asserting write-vs-read.

### QA-272127 — PRBB signature-block page split
Doc state: Recon+Rubric complete, **fix never applied by us** — Redmine Closed by Muhammad → colleague finished. Root (verified): 40 excess blank w:p in 2 templates; letterhead runtime-injected (logo claim ≠ template defect).
1-step catch: re-derive evidence (fresh DB/API) before restating prior-session screenshots as fact.
→ **learn-from-fix candidate** (diff colleague's shipped fix vs our locked spec).

### QA-272329 — PRBB SKM Negeri=11
Doc state: fix designed (+1 line at `PelupusanExcelReaderHelper.java:1412`), **not applied by us** — Redmine Closed by Amila. Root verified (8-of-9 convention grep).
1-step catch: in-file grep for `new PelupusanHakmilikVO()` at Phase 0.
→ **learn-from-fix candidate**.

### QA-273294 — PT/SKM validation gap
Doc state: investigation-only, candidate A (90%) never applied by us — Redmine Closed by Mohamad. Roots verified: no PT branch in `verifyCurrentLangkah()`; counter-PT pra-less write path.
1-step catch: the named-but-never-run breakpoint at `PelupusanSpocService.java:940`.
→ **learn-from-fix candidate**; Issue-1 carrier hypothesis unresolved — reopen if BA returns.

### QA-273921 — PPTPB Kertas nested table
Closed clean (shipped `mlk/release/1.3.3`). Root: CC-table-in-cell breaks server renderer (Word auto-repairs, e-Tanah doesn't). Residue: owner-data verify script never delivered (rule since banked as `feedback_template_ticket_data_patch`); multi-owner path data-untested.

### QA-275319 — PRBB flow re-route
Closed by みや re-altering the flow. Knowledge already banked (FLOWABLE-KNOWLEDGE §6b/§6c). No new residue.

### QA-272574 — PYSKT wrong template (PTG-shape at PDT)
Reassigned to Aaron 2026-08-03 pre-Apply; our deliverable = the doc. Root verified: `TemplateConfig.java:518` no office axis. ⚠️ PRBB regression residue (`TemplateSuratKeputusanTangguhPRBB.docx` orphaned by `10f1e7e7a1`, #247066) — separate-ticket candidate, みや to raise.

## Standing residue extracted (cross-quest, actionable)
1. `patch-273461.sql` PROD run — still owed (release-team attachment).
2. 273461 superseded branches v1/v2/v3 — deletion candidates, みや's nod.
3. PSBS running-number case-arm missing — latent throw, own ticket when PSBS goes live.
4. 274182 write-path container-per-save — own ticket (recurrence cause).
5. PRBB tangguh template regression (#247066 orphan) — own ticket, みや to raise.
6. 273294 Issue-1 carrier — reopen on BA return.

## Refinement proposals (→ slips type=proposal, strongest 5)
See slips ledger rows this date: branch-ledger content-hash at baseline recon · DB-first-before-Rubric gate · lifecycle-trigger-point line in logic-blast-radius · compile-the-merge-commit before deploy-ready · claim-verification extension to in-flight mechanism claims.
