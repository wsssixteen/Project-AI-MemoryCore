# rootcause-format

**What fires when**: Stop — quest hand-back emit carries a Root cause row with real content

**Contract**: block when the Root cause text contains dashes or semicolons; enforce short plain ASD-STE100 sentences

**Layer choice (Rule 7)**: TODO(forge): hook-only | skill-only | hook+skill — justify.

**Trigger moment (Rule 8)**: TODO(forge): justify this is the LEANEST trigger.

**Observability**: every fire appends to `domain/rootcause-format/log.jsonl` — TODO(forge): state what each line carries so an audit can read the fire history.

**state-scoped**: TODO(forge, Rule 11): `yes, keyed by <X>` | `no, state-agnostic`.
