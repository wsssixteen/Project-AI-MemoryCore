# 2026-08-28 (Fri)

## What happened
- Reopened **QA-277309** on a BA concern: was the "Ulasan Jabatan Teknikal" popup 7000-char fix covered system-wide?
- Swept every editable JT-ulasan surface across pelupusan / common / awam. Found **4** editable inputs, all writing `umm_a_jabatan_teknikal.ulasan` (varchar 7000). The pelupusan popup is a shared composite component → one edit covers 4 screens.
- Extended coverage to the **AWAM portal** page (`UlasanJabatanTeknikalForm.xhtml:229`, online JT officer) — committed `6abad84670` → int-env `4ec0f90526`. Reverted the pembangunan `bgn` file (wrong module, multi-state).
- Common team (Arkan) released the utiliti + Issue-2 fix as `etanah-common 1.5.2-MLK.beta.patch4`; bumped pelupusan's int-env pin (`e17c497870`, verified linear superset over 1.3.9-MLK.beta.patch1).
- miya deployed pelupusan + awam to int-env, tested all surfaces, passed to BA. Standby for rework.
- Shipped 3 refinements: commit-gate Check 1 (green build OR local-test), deploy skill prompt-value `bash` blocks, story-diagram-mandatory memory.

## What I learned
- Issue 2 (save flip) is **2022-origin** (arifin `f971b73c6e`), not a recent regression — a long-latent two-writer stale-read that only surfaced when the same ulasan was edited on utiliti AND viewed on the pelupusan popup.
- AWAM is multi-module + multi-state: the `bgn` file was pembangunan (KDH/MLK/TRG), not PT — module + state scope must be checked before editing a shared awam file, not just the entity/DB binding.
- A common-side fix reaches an env via a **version-pin bump**, not a ticket branch — no merge, just commit+push the pom pin to the env branch.

## Reflection
- Rough session on friction: the gate stack (commit-approval, compile-gate, local-test, merge-surface) fired repeatedly on a trivial 1-line xhtml change and the goal-hook ↔ commit-approval deadlock cycled many turns — miya said it was "tiring." The commit-gate Check-1 change is a start; gates tuned for `.java` should not fire identically on a 1-line template edit. Worth a broader look at trivial-change fast-pathing.
