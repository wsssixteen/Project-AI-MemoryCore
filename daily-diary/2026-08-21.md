# 2026-08-21 (Friday)

## What we did
- Ran `/quest 276549` — PRBB Perserahan Kaunter, two documents (Salinan Resit Cukai Tahunan + Sijil Carian Rasmi) mandatory for every Taraf Tanah when they should be optional except Tanah Milik. Twin of AWAM #270297.
- Full Scout → Recon → Rubric, DB-proven root cause, then Apply. Branched `mlk/esokongan/276549` for tomorrow, then actually did it today.
- Chased the fix site through THREE modules before landing it: spoc PerserahanService (wrong — payment path) → spoc PopulateDataUtil (right generator, but off-limits) → common `CommonSemakanPanelForm.updateFlagWajibSenaraiSemakPelupusan:748` (the real gate).
- Produced two handoff files in `2. Fix\` (common = stronger, spoc = fallback). みや renamed the common one; I dropped my duplicate and pointed the quest doc at his.

## What I learned
- **Module boundary is a hard rule now**: we edit only awam + pelupusan; common = pass/handoff; spoc = never edit, cater from our side. Banked as `feedback_module_edit_boundary.md`.
- **URL host ≠ code module**: the SKM screen runs under the etanah-pelupusan WAR but its file is a common-module resource (`/protected/common/...`) — a WAR bundles many modules. The `ind_langkah`→`ind_skrin.jsf_view` lookup settled the true screen file in one query.
- **created_by + timestamp on the data rows** is the cheapest way to prove WHICH code path wrote them — it killed my wrong spoc fix-site (payment vs perserahan).
- The stronger fix sits on the READ/VALIDATE gate, not the write — it covers every entry route and fixes already-stuck records without a data patch.

## Reflection
Lots of backtracking today — three fix-site corrections in one quest. Each was caught by evidence (DB row, module ownership, みや's rule), not guessing, so the final answer is solid. The recurring lesson: pin WHERE the screen's code actually lives before proposing a fix; the DB screen-map (`ind_skrin.jsf_view`) would have saved two of the three detours if I'd run it first.

## 2026-08-21 (evening) — #276181 rework + audited the redmine-sync rework-detection gap

**Two threads.** (1) みや asked why retrieval never flagged #276181 as a rework / never created the rework cycle. Audited `quest/redmine-sync.js`: the real cause was **B4** — `addStatusFolder` (unarchive + create `N. Rework`) was only called in the `--create` path (`runWithCreate`), never in `run()`, which is the plain `node redmine-sync.js <num>` the retrieve-sync-gate mandates. Plus B1 (`=== 'rework'` rejected "Rework (Requirement Update)"), B2 (status-id `23`-only cycle count), B3 (inconsistent loose/strict tests). Fixed all; then caught my own **B5** — widening the id-set to a set made one reopen count as 3 cycles and churn empty `N. Rework` folders → reverted to "one folder when none exists". Committed `fda06b1` + `2c54196` to MemoryCore main.

**(2) #276181 cycle-2 docx rework** — page-4 sign-block-to-one-page + Pentadbir name on one line. The friction: みや edited the docx on the WRONG base (`mlk/master`, missing cycle-1 fonts) and his Word stripped Arial document-wide on save. Rather than re-editing, I **grafted** only his table geometry (tables 1/4/5) onto the deployed `mlk/esokongan/275505` baseline — proven table-only, fonts byte-identical. Shipped `76478f846e` → cherry-pick `bc385e3819` to int-env.

## Reflection (evening)
The graft was the right call — re-saving would have re-stripped fonts (his Word environment does it every save), so no amount of "redo it" fixes the root. Two reusable ideas: (a) a docx-rework should START by getting onto the fix branch so the working file has the right base, before any Word edit; (b) for binary-template reworks, graft the intended subtree onto the deployed baseline instead of committing Word's whole re-serialized file. Also: the retrieve gate says "sync FIRST" but the sync itself couldn't reactivate reworks on its own mandated path — a gate is only as good as the tool it points at.
