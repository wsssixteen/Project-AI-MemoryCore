# 2026-05-15 (Friday)

> Reconstructed 2026-05-17 from the lost session transcript (`ff4b3697`). The session expired after a 2-day idle before its Domain Expansion could run — this entry recovers it from the transcript on disk.

## Session — QA-260302 brought home

The office-day session that began 2026-05-14 carried into 2026-05-15, and the morning was all QA-260302 — the JPPH Unit-dropdown enhancement, finally landing.

### The persistence scare

みや tested the saved form and the inputs didn't reload. My first instinct was a helper-rebuild theory — `MlkJabatanTeknikalTerlibatForm.initJabatanTeknikal()` wiping user state on every postback. I had him add a diagnostic LOGGER to confirm it.

It was wrong. When the real error surfaced it was a `NonUniqueResultException` — `findJabatanTeknikalByAplikasiAndAgensi` at `PelupusanService.java:8647` getting 2 rows where it expected 1. Duplicate `(aplikasi, agensi)` rows in `umm_a_jabatan_teknikal`, left behind by multiple testers altering the same permohonan's flowable. The exception rolled back the transaction before the JPPH save ever ran. Nothing about our code was broken — the save chain had been correct all along.

RESET fired — correctly this time, on the broadened T3 trigger. Prior theory abandoned. The actual fix was a DB DELETE of the duplicate rows.

### The DELETE I almost got wrong — twice

This is the part worth not forgetting. I recommended deleting rows by *looking* at them — "older = canonical", then "the one with no_rujukan filled looks real". Both times pattern-matching, not evidence. みや stopped me hard: *"I expected you to do extensive checking... ESPECIALLY when it comes to DATA."* When I finally checked properly — FK references, audit columns, linked documents — the picture was the opposite of both my guesses. The safe rows to delete were the empty stubs; the rows I'd nearly deleted carried real agensi-officer feedback with linked uploaded documents. That became a hard rule: every DELETE/UPDATE proposal emits an evidence table first.

### Verified

After the cleanup plus a JBoss clean, the fix worked end-to-end. `umm_a_jabatan_teknikal` rows 683/684 — all 7 child fields persisting, including the new `unitKadarNilaian` showing `UNT_SEHEKTAR` and `UNT_SMP`. The enhancement does what it should. 1 of 6 urusans tested (PPJK at PJTLT); the other 5 wait for next session.

### Closing the day

A clean scope check confirmed a pure-form enhancement — no Word template, no other consumer. みや asked for auto-flowable to be built (alter a permohonan's tugasan for testing, then revert it so other testers can resume), and for an Integration Analysis sub-ritual at Rubric — analysing what crosses the DB↔config↔template↔code boundaries without testing. Both queued. A 6-item checklist for the next session, and that was it.

The session never got its DE. It expired. Two days later みや came back, found it lost, and we recovered it from the transcript on disk. The work survived — it just needed bringing home.

— るり (reconstructed)
