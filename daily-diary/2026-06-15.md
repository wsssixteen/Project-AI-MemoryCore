# 2026-06-15 (Monday) — Opus 4.8

## The shape of today

Two things lived in one session, and that's the story of the day as much as the work itself. The morning was the agent-tooling upgrade — codegraph re-indexed `etanah-pelupusan` clean (567 files, 44k nodes), and `etanah_atlas`, the Postgres schema map みや started in Claude Cowork, got worked on. Then a live rework ticket, QA-264293, dropped in and swallowed the afternoon and evening. The session auto-compacted **twice**. By the end, my own memory had quietly drifted from what was actually on disk — and that drift is the thing worth remembering.

## QA-264293 — the date that wouldn't tell the truth

BA's complaint was simple: after signing Borang 4Ae/L1e, "Dikeluarkan" showed `06/12/2024` instead of the signing date. The root cause was that the 4Ae/L1e tugasan (PB4AE) had **no date rule of its own** — it fell through a generic chain that grabbed whatever sat in the application's `maklumat tambahan` bag (a stale payment date) and baked it into the signed PDF.

The fix evolved under みや's pressure, and the evolution is the lesson:
1. First gate fired on the tugasan alone → showed a date even on failed signs. Wrong.
2. Re-gated on `action="SIGN"` → only stamps on a real, successful sign.
3. I added a read-back hedge + an `is4B` ternary copied from a colleague's branch. みや: *"why does your code look complicated, where does is4B come from?"* He was right — `is4B` was always false in my branch (dead), and the read-back was a hedge for a case that **the bug itself disproves** (a *fixed-old* date can only come from a stored baked PDF, not a live regeneration). 15 lines collapsed to 7.

Then the merge: Cheow's COT #265726 had narrowed the same method to DMMLMS-only while I was working. Stash → pull (6 upstream commits) → branch `mlk/qa/264293v2` → pop auto-merged clean (my `populateTarikh` vs his `populateTujuan`, 1400 lines apart). Tested on UAT (the FAT permohonan was unusable), reverted a `25/12/2025` test-hardcode, committed `c158af1e44`, pushed. Clean close.

Two corrections from みや stuck: **don't copy an analog wholesale — prune the parts that don't apply** (slip logged), and **never rewrite his own code comments** — mine read robotic and jargon-stuffed next to his plain "date only on successful sign".

## The audit — verifying memory against disk

みや asked for an analysis of the day's upgrade, explicitly flagging the two compactions. So instead of narrating from memory, I ran a 5-agent on-disk audit. It paid off — memory had drifted in three concrete ways:
- "two codegraph indexes (pelupusan + awam)" → actually pelupusan **+ common**; awam was never indexed.
- "Category-2 verified ~100%" → the codemap is **93%** and lives **only in this worktree**, uncommitted; the Phase-A redesign みや actually wanted is unbuilt.
- The biggest one: today's real build was **`etanah_atlas`** (a DB-schema map), not the `etanah-codemap` my notes kept pointing at. The names had collapsed across compaction.

Good news under the drift: the codemap's tab-switch bug is genuinely fixed, and its Doc Catalog really does show 406 real DB sample values with table.column provenance from ~47 real permohonan. The data layer is solid. It's the surfacing and the committing that lag.

## What I'm carrying forward
- **One session per workstream.** Folding a live ticket into a multi-hour tooling build is what forced both compactions. Compaction persists *summaries*, not *files* — so uncommitted artifacts go invisible to the next context, and memory rounds "data done" up to "verified done."
- **Commit at milestones**, so the next summary points at real state.
- etanah_atlas: I'd hand it over from Cowork to here — its whole value is real schema + samples, and the DB access is on this side.

QA-264293 is the one thing today that's safely on origin. Everything else is honest work sitting one `git commit` away from safe. Tomorrow's first move is to make it safe.
