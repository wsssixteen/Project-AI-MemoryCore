# 2026-08-19 (Wednesday)

## What happened
- Adhoc investigation ADHOC-OPRBB-2026-1: OPRBB permit "Kuantiti Diluluskan" shows 0.00 / blank in Carian Pintas grid + AWAM "Maklumat Permohonan" popup, but shows 55000 Ketul correctly in the permit "Maklumat Jadual" + Borang 4Ce. Permit C02/2026/5 = PTMLK/02/L/OPRBB/2026/5, mlit, "Ganti Hari".
- DB spine (mlit, et_main_mlit): approved qty for all 5 OPRBB permits sits in `kuantiti_pengeluaran`; `kuantiti_dilulus` is null. Working analog PRBB fills `kuantiti_dilulus`, leaves pengeluaran null.
- Root cause pinned to `etanah-pelupusan\...\PelupusanService...` — actually `PelupusanLiteService.java:1080` OPRBB branch (`URS_OPRBB`) writes `setKuantitiPengeluaran(kuantitiAmbil)`, never `setKuantitiDilulus`. Broken read surfaces read `kuantiti_dilulus`.
- Delivered: data patch `patch-oprbb-kuantiti-dilulus.sql` for aplikasi 3408554 (self-column copy pengeluaran->dilulus + unit, idempotent guard, before/after SELECT). Code fix + remaining-4-row backfill deferred to the owning ticket.
- miya: this adhoc is under another Redmine ticket (number TBD). Archived at Phase 2.

## Decisions
- Recommended write-side fix (fill kuantiti_dilulus like PRBB) over read-side fallback — minimal, matches working analog, fixes all read surfaces at once.
- Patch scoped to the single reported permohonan only (other 4 OPRBB rows left until miya says).

## Learnings / friction
- Agent tool spawns blocked all turn by an erroring local hook (`agent-spend-gate` — "No stderr output"); did the code trace inline with Grep instead. Worth checking that hook.
- My active.txt block for the adhoc was dropped mid-session by a concurrent OneDrive/commit sync (459b8cb pushed by quest-bounty) — re-recorded the archive record directly in active-archive.txt. Known OneDrive-clobber class.

---

## Session 2 — bundle-Apply day: 275505+276181, two forge features, 275501 archived

- Morning: boot briefing + 3-ticket plan (quick-wins first). miya bundled 275505+276181 (same Kertas Pertimbangan PPTPB template; BA herself suggested one-ticket fix).
- /quest resume ran the full 1a/1b discipline: redmine-sync first, all 9 attachments opened (PDF annots, video frames — the video proved METER PERSEGI lives inside the luasSyor CC), blind re-derivation before opening the qa_doc, reconcile all STILL-HOLDS. miya then asked me to audit exactly that — verdict honest: substance green, echo-contract emit was late, checklist-show skipped.
- Applied 2 Java fixes compile-green (populateKeadaanTanah empty-key → tanahTek delegate + "tanah " prefix; populateLuasSyor uppercase → Meter Persegi). 3 template edits queued behind miya's OPEN WORD window — asked, never forced; still locked at DE.
- Feature ask → forged domain/template-cc-preflight (CLI+advisory hook; its first live run caught its own parser gap — literal-key puts) + domain/feature-creation (keyword → 9-step pipeline injection). Both evals 5/5. Quest skill gained the CC-PREFLIGHT pre-emit row.
- 275501: recognized as the ticket form of ad-hoc A12 (same NPE, same ID Rujukan 254883) — Phase 1 data-patch close + Phase 2 archive per miya; null-guard leg deliberately left with ADHOC-PRBB-2026-1. Register row moved on, bestiary family entry banked.
- Friction: awam-no-resit-gate errored 2×; DE 0b merge brought 16 main commits in (compile-gate exists after all — my "missing" read was pre-merge truth); 3 append-only-ledger conflicts union-resolved losslessly.
