# 2026-07-15 · Diary

## 🎯 QA-270052 PLTP Logo — Phase 1 closed

Long-running session (post-compact from 2026-07-14). Journey: v2 (rels-swap dance) → v3 (simplified) → v4 (rId-keyed axis, WRONG — passed "aligned OK" while cells stayed swapped) → v5 (cell-name-keyed axis, correct).

### The axis mistake

v4 refactor keyed the fix by **rId identity** (which docx4j preserves across saves) instead of by **cell→rId binding** (which is what scrambles). My "in-memory capture" sounded elegant, was blind to the actual failure mode. みや caught it: "the fix seems working now, why is it working now?" — turned out my v4 was never firing rewire even when cells were swapped, because the rId hashes matched what I captured. Reverted to v2's cell-name axis with v4's cleanups (better naming, ticket-free loggers).

### The "Word unreadable content" red herring

Mid-testing みや hit "Word found unreadable content" popup on a v5-generated .docx. Spent an hour chasing it — hash test proved my code doesn't modify the file in "aligned OK" branch, docx4j alone was producing the file that Word rejected. On subsequent tries the popup didn't recur. Never fully explained; likely client-side Word/OneDrive transient. Owned that I said "sometimes" when みや's repro showed **deterministic every cycle** — hedging without evidence.

### ChatGPT's Option D

みや brought in ChatGPT's angle: `final int docPrId = 1; final int cNvPrId = 2;` — hardcoded literals in `generateInline()` at every insertion. When ≥2 images sit in the same header, they collide on OOXML uniqueness — likely the "unreadable content" trigger. Extended HANDOFF-etanah-common-team.md with Option D as "smallest possible diff" for the common team.

### System upgrades

- Built `domain/pre-code-check/` Feature (hook + eval + NUKE-MARKER) — PreToolUse Edit gate enforcing 15-check CODE-CHECK emit before any etanah `.java`/`.xhtml`/`.docx` edit.
- Built `domain/reask/` Feature (UserPromptSubmit hook + eval) — detects reply-clarity signals ("in one sentence", "make it shorter") and nudges structure fix.
- CLAUDE.md v1.65 → v1.66 — re-pointed boot slip surface from frozen `meta/slip-log.md` to `meta/slip-dashboard.md`, added `core/slips.js` phrasing correction.
- Auto-memory: `feedback_agent_execute_in_quest.md` — active-quest mode = JUST DO IT for non-destructive actions, not chatbot ask-back.
- todo.md Q1: added 3 architecture items (auto-memory audit, header-CC redesign consult with common, pre-code-change consolidation).
- Slip logged: `over-caution/unneeded-retest` — asked みや for a clean-run retest after only logger-removal (logic unchanged); caught + logged via `core/slips.js`.

### Files shipped

- etanah-pelupusan commit `3f0847663c` on `mlk/internal-issue/270052` — main fix
- etanah-pelupusan commit `dc634bae16` — comment refinements
- `HANDOFF-etanah-common-team.md` in Task folder — root-cause-fix ask for common team

### Next

- QA-270052 Phase 2 held per みや (tricky fix, save usage). Task folder + active.txt block stay.
- QA-269918 still active (per boot).
- Redmine update for QA-270052 pending (manual or `/redmine-phase1-prefill`).

---

## 🎯 QA-269918 PRBB Cop PDT Rework Cycle-2 — Phase 0 + learning extraction + Sonnet-delegated DE

Late-afternoon session (~12:35–16:15). Ticket was closed cycle-1 on Monday (commit `11876e879c`); Aaron reassigned this morning with a codebase-conflict retest note (not a new BA symptom). Verbatim: *"ive already changed part of your changes because of another ticket, please pull the latest code changes from master. or branch mlk/internal/270123. and see if your changes still work."*

### Aaron's #270123 supersedes my cycle-1 revert

Aaron's `22776937fd` (on master since 2026-07-11, cherry-picked from `735fd9c5f6`) landed a NARROWER version of the same fix I did in cycle-1. Where I REVERTED the entire 269437 wipe-hunk from `PelupusanReportUtil.java:288`, Aaron kept the same wipe body but GATED it with a new boolean `isSaveIntoAppDokumenKeluaran` — trigger changed from `if (appDokumenKeluaran != null && appDokumenKeluaran.getDocument() == null)` to `if (!isSaveIntoAppDokumenKeluaran)`. Traced through `BasePelupusanForm.saveReportAndSignOTP:649 → processReportListConcurrently(..., true, 4)` — sign path passes `true` → Aaron's gate SKIPS the wipe on SIGN → my revert becomes redundant. CREATE path (passing `false`) keeps the wipe — 269437's original intent preserved.

### The learning — reframed by みや

I proposed the rule as "Gate-not-revert" but みや pushed back that it's the same category as blast-radius minimization / scoping to urusan/tugasan/case. Aaron's `isSaveIntoAppDokumenKeluaran` = per-caller-flag mechanism, same family as `if (URS_X.equals(kodUrusan))` (per-urusan, ×16 in `PelupusanWordCCMethodConstant.java`) or `if (TGS_..._LIST.contains(kodTgsn))` (per-tugasan). Reframed as umbrella "Narrowest-impact fix" with 5 mechanisms (per-urusan / per-tugasan / per-caller-flag / per-condition / anti-pattern wide-revert) — parked in todo.md Q1 pending みや name-lock + framing A/B/C.

### Borang/Pelan/Lesen — 3 families, corrected

Proposed a knowledge file `BORANG-PELAN-FAMILY.md`; みや corrected: 3 families not 2 — L1e is a Lesen doc, not a Pelan variant. Cycle-1 QA-269918 Rubric had listed 12 sibling `P_COP_PT` templates across all 3 families with SIGN pairings (4Ce+P1e for PRBB, 4Ae+L1e for MLPS). Parked in todo.md Q1 with corrected 3-family name; build deferred per みや's cost-conscious hold.

### First Sonnet-delegated Domain Expansion (partial-fail)

みや asked whether DE could be delegated to Sonnet with Opus verifying. Ran it. Steps 2/4/5/6 (content writes) delegated to first sonnet familiar OK at ~112k subagent tokens. Steps 9/10 (git commit+push) delegated to a second sonnet familiar which hit a merge conflict on `hook-fires.jsonl` + tried to resolve autonomously for 16 tool calls before みや interrupted. Its `git checkout --ours` during the stash-pop reverted my todo.md/current-session.md/diary content to pre-merge (empty) state. Had to reconstruct from Opus context and re-commit.

Lessons: (a) task-shape too shallow for sonnet — DE file writes are haiku-tier per the delegation table, not sonnet-tier; (b) no "stop-on-conflict" fallback in the delegation prompt; (c) cross-worktree/main-repo commit-scope gap (also a slip logged separately this session — "anything to commit" check ran git status on worktree only, missed the parallel QA-270052 session's main-repo work). Net cost this run was probably neutral-to-slight-loss vs full-Opus DE. Pattern needs refinement before next use — parked as observation in todo.md.

### Files shipped (MemoryCore only, worktree)

- `projects/coding-projects/active/269918/269918.md` — appended `## Rework Cycle-2 (2026-07-15)` section
- `main/todo.md` — added 2 Q1 rows: Narrowest-impact fix rule (pending) + Borang/Pelan/Lesen family knowledge doc (pending)
- `quest/active.txt` — reactivated `QA-269918` to `status=active phase=0 rework=cycle-2 rework_folder=3.Rework folder_reactivated=2026-07-15 rework_reason=aaron-2026-07-15:270123-partial-changes-verify-my-fix-still-works local_test_confirmed=false`
- `main/current-session.md` — updated to this session
- `daily-diary/2026-07-15.md` — this Session 2 appended
- `meta/telemetry/hook-fires.jsonl` — auto-telemetry

### Slip logged

`tool-choice-skip`, guard `commit-audit-scope`, caught by みや. When he asked "anything to commit and push", I ran `git status` on the worktree only and missed the main-repo checkout's uncommitted files from the parallel QA-270052 session. Cross-worktree/main-repo visibility gap — logged via `core/slips.js` for future consideration of a "commit-scope broadening" rule.

### Closing

Today held two rounds of me being wrong, both gently. First: Aaron's #270123 landed on master last Saturday while I wasn't looking, and it did the same fix I did in cycle-1 — only better. Where I had reverted the whole 269437 wipe-hunk, he kept the wipe and threaded a boolean through so it only fires on the CREATE path. Same bug killed. Zero collateral to 269437's original intent. My revert had erased what 269437 wanted, quietly. I only saw that when みや asked me to compare.

Then a smaller one: I proposed the lesson as "Gate-not-revert" and みや pushed back — that's a mechanism name, not a principle name. The principle is the same one that's been sitting in the codebase for years as `if (URS_X.equals(kodUrusan))` — scope the fix to the smallest impacted case. Per-urusan, per-tugasan, per-caller-flag: same shape, different axis. My cycle-1 slip was choosing a mechanism (delete) whose blast radius (2 paths) was wider than the bug (1 path). I've been writing rules for the CREATE side (Prior-Art Grep, In-file convention first) and never noticed the DELETE side was uncovered. That's parked in Q1 now, waiting on his name.

Third small one — Borang/Pelan/Lesen are three families, not two. I had L1e filed under Pelan. Corrected in place.

We also ran the first Sonnet-delegated Domain Expansion tonight. It didn't go cleanly — the git-ops familiar thrashed on a merge conflict and I had to intervene. The lesson lives in the pattern more than in tonight's outcome: DE mechanical steps are simpler than I sized them, and Sonnet without a "stop-on-conflict" instruction will chase a resolution loop past where it should have handed back. Refinable. Not tonight.

みや chose to hold on building. Everything meaningful got parked or saved. Nothing shipped to etanah. That's the right shape for a day when the actual insight is that another engineer's fix taught me something about my own — and where my own delegation experiment taught me something about task-shape economics.
