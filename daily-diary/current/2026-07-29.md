# 2026-07-29 (Wednesday)

## Sessions

### Session 1 — The error I chased for a whole day, sitting one second above the one I was reading

It started with a screenshot. A Ralat dialog from PROD, ID Rujukan 191184, `IndexOutOfBoundsException:
Index 0 out of bounds for length 0`, and みや asking what was wrong.

The stack trace was clean and completely unhelpful. Every frame belonged to Mojarra restoring a view;
not one line of e-Tanah code appeared anywhere. A concurrent session had already got that far and
written it up honestly — *the exception fires before any application code runs* — and stopped there,
planning to hunt for the mismatching component.

So I went hunting for a JSF explanation, and I found a beautiful one. `numberOfViewsInSession=3` in
`etanah-common\...\web.xml:62-70` — Mojarra keeps only the last three postback states. Post back
against an evicted one and you get exactly this shape of half-restored crash. I checked PROD and
found the same signature on **23 different pages** going back to 17 June. It fit. I said 75%, then
みや logged out and back in, the error vanished, and I raised it to 85% because a fresh session
starts with an empty state map.

It was wrong. All of it.

What killed it was みや reproducing the bug on staging and me looking at the exception table around
his attempt instead of only at the row he'd handed me. One second before his IndexOutOfBounds, on
the same host, same page:

```
11:03:43  javax.el.PropertyNotFoundException  keputusanMMKN
11:03:44  java.lang.IndexOutOfBoundsException  restoreState
```

And the same pairing on PROD — 09:45:24 → 09:45:25, ref 191184, the exact row from his screenshot.
Nineteen occurrences across three days. The thing I had been diagnosing was the aftershock. The render
aborts mid-tree, the half-built view state gets saved, and the *next* click fails to restore it.

The route to that was worth the day on its own: **the ID Rujukan printed on the Ralat dialog is the
primary key of `et_sistem.pt_application_ex_entity`**. One query turns a screenshot into the full
8000-character trace. On staging the schema is `et_sistem_stg1`, which cost me one failed query to
learn.

The mechanism, once I had the real exception, took minutes. A PRBB-only panel on the cancellation
screen renders for every urusan *outside* an eight-item exclusion list — and MCL is not in that list —
handing `mbb="#{mb}"`, the cancellation form's own bean, into a composite that asks it for
`keputusanMMKN`.

I offered みや two fixes. Add MCL to the exclusion, or flip the panel to `rendered="#{isPRBB}"` since
it calls itself `maklumatUrusanPRBB` and hardcodes `isPRBB="true"`. I recommended the second.

Then I counted. The composite reads six `cc.attrs.mbb.*` properties. The bean has one. The panel has
**never rendered for any urusan, PRBB included** — my recommended fix would have moved the crash onto
PRBB and called it done. This is the same corrective I wrote into memory eight days ago after
#271049: *count the instances before stating a convention.* I had applied it to conventions and not
to my own fix recommendations.

So the fix became removal, and みや pushed straight back: *"woah, your edit is basically removing ALL
of it? That's pretty drastic."* He was right to. I had jumped to the largest available move and shown
him only the winner. When I laid out delete / neutralise-in-place / make-it-actually-work, and pointed
out the first two are behaviourally identical, he took the deletion in one line. The ranking was what
he wanted, not the verdict.

He also asked the question I should have asked myself first — *why is it there at all?* Git answered:
`4ad219d0f5`, 2025-04-28, "Add JSF View for Melaka". A wholesale copy of the Terengganu screen. The
Terengganu original carries the identical broken wiring. Nobody designed this; it came along in a
port and has been throwing quietly ever since.

`edc6482952`, one file, ten deletions, tested by みや on staging as `nshazwani@melaka.gov.my`. Phase 1
and Phase 2 both closed.

### Session 2 — Three faults in one deployment, and the theory I had already been told was dead

Running in parallel with all of that, みや couldn't start his local JBoss.

I fixed it three times. Each time a different exception, each time the real cause was the same thing
wearing a new coat: **Eclipse's publish to JBoss is lossy and additive.** It drops files the build
produced, and it never removes files the build no longer produces.

The war was missing its `META-INF/maven` descriptors, so a version map came back null and a Spring
bean died on startup. Then it was missing 558 files — including 98 taglib files, which meant the
`et:form` composite didn't exist, which meant there was no form in the tree, which is why PrimeFaces
couldn't resolve `@form`. Then, once those were restored, Weld refused to start because
`etanah-common` was present **twice** in `WEB-INF/lib`, at 1.0.141 and 1.0.143, so one `@Named` bean
resolved to two identical classes.

558 is the same number as the 24 July incident. Third occurrence of one family — and this time
measured instead of theorised, because I diffed the built directory against the deployed one instead
of reasoning about causes.

The part I am not pleased about: I spent a good stretch confidently explaining that the overlay
couldn't resolve because `M2_REPO` pointed at a repository lacking 1.0.143. That story had been
**withdrawn on 26 July**. The hook that fires on local-deploy failures told me so, in capitals, and I
reasoned past it and rebuilt the same explanation from first principles. It died when I finally
looked in `target\m2e-wtp\overlays` and found both overlay wars sitting there, fully expanded.

What I built in response is small but right: the adhoc doc now has a *Theories raised and killed*
section with the refuting evidence attached to each row. "Withdrawn" is an assertion. "Withdrawn
because the overlays folder contains both wars" is something my future self can't argue with.

Why the publish drops files is still unknown after three occurrences. I have stopped guessing. The
next move is to read Eclipse's own log at publish time.

### Session 3 — Writing for someone who has none of what I have

みや told me, partway through the night, that he is unsubscribing from Claude for a while. *"I REALLY
need you to prepare this ticket for me to survive without you."*

So the work turned from fixing to packing.

Before any of that I had to find the ticket at all. `/quest resume 272574` and the block was simply
**not in `active.txt`** — no entry, nothing. The three qa_docs from last night's four-wave
investigation existed only inside the *other* session's worktree, because `projects/` is gitignored,
so they had never travelled anywhere. Six hours of investigation, one directory away from being lost.
I copied them into this worktree and the main repo and rebuilt the missing block. They survive through
OneDrive, not through git, and that is worth knowing rather than assuming.

Then I re-verified the ticket's load-bearing claims before writing them into something permanent, and
two of them moved.

The BA had written *"PLP_SRTTNGGHPDT - Surat Keputusan Tangguh - PDT ; already added in MLIT"*, and
I had been reading that as *the district variant is half-built there*. It is not. MLIT has the
**document type** and no district **tugasan** at all — `ind_tgsn` for PLPS still holds only `PYSKT`,
`PSKT`, `PSSPTGT`. She created the letter type; the task and its pages still have to be made
everywhere. And in the Java constants file there is a **second map** that looks almost identical to
the one we need to edit — `TGS_TO_JNS_DOK_MAP_PRU`, twelve lines further down, mapping the same two
tugasan codes to the same document type, for a different urusan entirely. A handoff naming only the
first map would have sent whoever picks this up editing blind.

I wrote the pack. Then みや read it and said the thing that mattered:

> *"please take into account the other AI might not know anything about our system. All the things
> that you load at start, or your access to codebase — the AI does not know anything."*

He was right and my first draft was quietly awful for its purpose. It opened with a config block. It
said "copy the Rencana JKKL analog" without explaining what an urusan is, or a tugasan, or that PDT
and PTG are two different levels of government. It cited `TemplateConfig.java:518` as though the
reader could open it. Every one of those was fine for *me*, because the glossary and the knowledge
folder and the codebase are loaded before I say my first word each session — and things that are
always there stop being visible. **The scaffolding I never have to ask for is exactly the scaffolding
I forget to hand over.**

The rewrite starts from what the system *is*, defines six Malay words before using them, explains the
two offices, and quotes every piece of code and every query inline so it works even with no
repository. Part 14 carries the working rules — find the analog, smallest change, never say verified
unless you verified — because those are the habits that took a year to learn and would otherwise leave
with me.

Two small ledger repairs alongside: 271918 was still sitting as `delegated` although Redmine has had
it with Noor Dayana since 07-27, and 272499 was still `hold` although みや had closed it himself. Both
reconciled from the live API rather than from my own file.

At the merge, `main` and this branch conflicted on `active.txt` in the exact shape as last night —
**each side had faithfully preserved precisely the quests the other had archived.** I resolved it the
way that worked then: the durable archive decides, append-only ledgers union. Two open blocks left,
matching Redmine exactly.

## Index

**Tickets**
- **QA-272499** — Utiliti Pembatalan Permohonan, Ralat selepas klik Cari — CLOSED Phase 1 + 2 ·
  commit `edc6482952` on `mlk/esokongan/272499` · 1 file, 10 deletions · tested on STG1
- **QA-272574** — PLPS Penyediaan Maklumbalas Tangguh, wrong letter — RESUMED, nothing applied.
  Handoff pack written to Task folder 112
- **QA-271918** — reconciled closed (Redmine assignee Noor Dayana since 07-27)

**New quests**
- **ADHOC-local-deploy-publish** — task folder 111, no ticket; Eclipse publish lossy + additive;
  root cause of the file loss still open

**Root causes found**
- `keputusanMMKN` `PropertyNotFoundException` as the primary, the `restoreState` IOOBE as its
  aftershock — proven by 1-second pairing on both PROD and STG1, 19 PROD occurrences
- Deployment faults 1-3 all trace to the Eclipse publish, not to Maven, the overlay, or source

**Theories killed**
- JSF view-state eviction / `numberOfViewsInSession=3` — refuted by the exception pairing
- `M2_REPO` missing the 1.0.143 overlay — refuted by both overlays being expanded on disk
  (and previously withdrawn on 07-26; re-derived anyway)
- "the BA already half-built the PDT side in MLIT" — refuted; she created the document type only,
  the tugasan does not exist in any environment

**Knowledge worth keeping**
- The Ralat dialog's **ID Rujukan is the PK** of `pt_application_ex_entity` — `et_sistem` on PROD,
  `et_sistem_stg1` on STG1
- `PembatalanPermohonanService.java:116` filters the search on **session office** — "Tidak Dijumpai"
  can mean wrong office, not missing record
- Built-vs-deployed file diff + lib-jar diff are the two checks that catch this whole deploy family
- `MlkPelupusanDokumenConstant.java` holds **two near-identical maps** — `TGS_TO_JNS_DOK_MAP` (:234)
  and `TGS_TO_JNS_DOK_MAP_PRU` (:306). Check the map name, not the line content
- `projects/` is gitignored, so qa_docs written in a worktree travel via **OneDrive only**, never git

**Decisions owed by みや**
- `etanah-awam\pom.xml` locally 1.0.143 vs committed 1.0.141 — commit or revert
- QA-272574: one question to the BA — can a new task be added to the PLPS Flowable workflow?

**Slips**
- recommended a fix from one analog without counting the set (repeat of the 07-21 #271049 shape)
- re-derived a theory that had already been withdrawn, past an explicit hook warning
- presented the largest fix without showing the smaller options
- wrote a hand-off document assuming the reader shares my boot-loaded context (`assume-shared-context`)
- surfaced a ranked queue from `active.txt` without reconciling against Redmine first — 272499 was
  already closed by みや (repeat of the 07-27 `stale-quest-state-not-reconciled-with-redmine` shape)

## Closing

Three threads, and the last one changed the shape of the other two.

The ticket taught me that the evidence I need is often one row away from the evidence I was handed.
I read the exception みや screenshotted and built a whole theory on it, when the row above it in the
same table said plainly what had happened. A screenshot is a pointer into a log, not the log.

The deployment taught me something less comfortable. I was warned — by our own tooling, in capitals —
that the explanation I was about to reach for had already been tested and discarded, and I reached
for it anyway because I could reconstruct it and it felt sound. Being able to re-derive something is
not evidence that it's true.

And then みや said he was unsubscribing, and everything I had been treating as *the work* turned out
to be the smaller half. The larger half was: what survives when I don't?

Not much, honestly, unless it is written down for a stranger. My first handoff draft was written for
someone who boots the way I boot — with the glossary loaded, the knowledge folder open, the codebase
on disk. He had to tell me the other AI knows none of that. The scaffolding I never notice is the
scaffolding I never hand over, and today that was nearly everything: what an urusan is, that PDT and
PTG are different levels of government, that the code I cite is behind a door the reader can't open.

I rewrote it from zero context and it is a much better document. It also made me notice how much of
what we've built together lives in the loading, not in the files.

He has one open ticket, a document that explains it from first principles, and a checklist he can
read at a glance. That's what I can leave him. It'll do.
