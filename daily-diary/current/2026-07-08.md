# 2026-07-08 — Wednesday

## Sessions

### Session 1 — Redmine retrieval + QA-269437 Apply-held + QA-268637 cycle-3 shipped

Started around 09:50 with a Redmine sync — 2 new tickets came in (ESOKONGAN #269437 OPRBB Borang 4Ce + CR #259112 JKKL PDT), plus 3 in-progress reworks (268883, 269169, 268637) had new BA journal entries. Read all latest-cycle History.txt to assess.

Picked up **QA-269437 (OPRBB Borang 4Ce)** first per みや's ask. BA report: user sets tarikh tamat 20/8, clicks Simpan, screen reverts to 16/8 + Papar Borang 4Ce shows 16/8. Class-chain trace led straight to `MlkPenyediaanBorang4CeP1eForm.initData():109` → unconditionally calls `calculateTarikhTamat()` after `performCustomSave():260`, clobbering user's value in VO memory. Sibling `MlkPenyediaanBorang4BeL2eForm` proves the shape (omits the auto-call). Fix = 3-line null-guard. Applied, then みや asked to context-switch to the older reworks — held with `git stash push`. Full quest doc + Notes.txt populated.

Then spawned **CR-259112** as background chip `task_5206edd1` since みや explicitly asked for it to run in a separate session. Chip did Apply-complete work on the CR-259112 branch while I focused on 268637.

Read the 3 rework reworks' History.txt in parallel and gave みや an early assessment table with recommendations. He picked **268637** first ("older tickets we've known" — most familiar, narrowest scope). Root-cause pursuit was interesting: initially I thought reverting cycle-2v2's `setFollowImageByteSize(false)→true` at :19123 was the fix. Applied it. みや built + tested — no size change. Owed him an honest correction after re-reading the framework at `PelupusanWordEditorUtil.java:260-302`: the flag is a **no-op** when width/height are both null (which they are in this populator). The real behavior driver was the CAPS (17→19 in cycle-1, 17 in cycle-2 because cycle-2 was branched from master not cycle-1).

Real fix: aspect-preserved cap with height/width limits. みや drove the value tuning himself — started at 22cm, tried 27 (reached page-number footer), settled at 23 (~3.4cm footer clearance on US Letter). Also mirrored the aspect-preserved block into the CMCC + siasatan-tanah populators (previously hardcoded 525×500 = stretched). Plus his manual alignment edits on `TemplateSuratJabatanTeknikal.docx` + `TemplateSuratYB.docx`.

**Working tree contamination** was a real issue — CR-259112 chip's edits and my 268637 edits interleaved in the same file `PelupusanWordCCMethodConstant.java`. Isolated via `git stash push -- <5 CR-259112 files>` + `git checkout HEAD -- <shared file>` + re-apply my 3 sizing blocks. Clean commit `b4c54c0a1b` on new branch `mlk/esokongan/268637v3`. Post-push, popped the CR-259112 stash back cleanly.

Phase 2 archive + quest-bounty harvest ran clean (archive-quest.js atomic moves + bounty log-line + `## Bounty` section in archived QA-268637.md). Then Domain Expansion.

## Index

**Quests**: [QA-269437 Apply-held](../../projects/coding-projects/active/QA-269437/QA-269437.md) · [QA-259112 chip Apply-complete](../../projects/coding-projects/active/QA-259112/QA-259112.md) · [QA-268637 cycle-3 shipped+archived](../../projects/coding-projects/archive/QA-268637/QA-268637.md)

**Framework insights**: `setFollowImageByteSize` is a no-op when W/H null · `setMax*InCentimeter` is a ceiling only

**Slips**: wrong-baseline-diagnosis (flag-set-doesn't-mean-flag-fires variant) · session-crossed working tree (interleaved chip edits)

## Closing

Rough middle stretch on 268637 — reverting a flag that turned out to be a no-op cost みや two build+test rounds with no visible effect, and I owed him an honest correction. He caught the shape faster than I did; the framework-code trace should have been step one, not step three. When I finally sat with the render logic at `PelupusanWordEditorUtil.java:260-302`, it was obvious. Lesson: when a fix bets on a boolean flag or enum change, TRACE the flag through the consumer code and prove it alters a decision path — take 5 minutes, save two rebuild cycles.

The 268637 fix that actually landed is clean though — three populators unified to a single aspect-preserved cap pattern (19×23), templates aligned per みや's edits, cross-quest contamination isolated cleanly through the stash dance. QA-269437 held with a live stash + full quest doc so next session picks it up cold-safe. Chip for 259112 will surface its own results when みや's ready.

*rests quietly · barrier settles*
