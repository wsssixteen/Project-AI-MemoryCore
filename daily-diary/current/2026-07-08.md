# 2026-07-08 — Wednesday

## Sessions

### Session 1 — Redmine retrieval + QA-269437 Apply-held + QA-268637 cycle-3 shipped

Started around 09:50 with a Redmine sync — 2 new tickets came in (ESOKONGAN #269437 OPRBB Borang 4Ce + CR #259112 JKKL PDT), plus 3 in-progress reworks (268883, 269169, 268637) had new BA journal entries. Read all latest-cycle History.txt to assess.

Picked up **QA-269437 (OPRBB Borang 4Ce)** first per みや's ask. BA report: user sets tarikh tamat 20/8, clicks Simpan, screen reverts to 16/8 + Papar Borang 4Ce shows 16/8. Class-chain trace led straight to `MlkPenyediaanBorang4CeP1eForm.initData():109` → unconditionally calls `calculateTarikhTamat()` after `performCustomSave():260`, clobbering user's value in VO memory. Sibling `MlkPenyediaanBorang4BeL2eForm` proves the shape (omits the auto-call). Fix = 3-line null-guard. Applied, then みや asked to context-switch to the older reworks — held with `git stash push`. Full quest doc + Notes.txt populated.

Then spawned **CR-259112** as background chip `task_5206edd1` since みや explicitly asked for it to run in a separate session. Chip did Apply-complete work on the CR-259112 branch while I focused on 268637.

Read the 3 rework reworks' History.txt in parallel and gave みや an early assessment table with recommendations. He picked **268637** first ("older tickets we've known" — most familiar, narrowest scope). Root-cause pursuit was interesting: initially I thought reverting cycle-2v2's `setFollowImageByteSize(false)→true` at :19123 was the fix. Applied it. みや built + tested — no size change. Owed him an honest correction after re-reading the framework at `PelupusanWordEditorUtil.java:260-302`: the flag is a **no-op** when width/height are both null (which they are in this populator). The real behavior driver was the CAPS (17→19 in cycle-1, 17 in cycle-2 because cycle-2 was branched from master not cycle-1).

Real fix: aspect-preserved cap with height/width limits. みや drove the value tuning himself — started at 22cm, tried 27 (reached page-number footer), settled at 23 (~3.4cm footer clearance on US Letter). Also mirrored the aspect-preserved block into the CMCC + siasatan-tanah populators (previously hardcoded 525×500 = stretched). Plus his manual alignment edits on `TemplateSuratJabatanTeknikal.docx` + `TemplateSuratYB.docx`.

**Working tree contamination** was a real issue — CR-259112 chip's edits and my 268637 edits interleaved in the same file `PelupusanWordCCMethodConstant.java`. Isolated via `git stash push -- <5 CR-259112 files>` + `git checkout HEAD -- <shared file>` + re-apply my 3 sizing blocks. Clean commit `b4c54c0a1b` on new branch `mlk/esokongan/268637v3`. Post-push, popped the CR-259112 stash back cleanly.

Phase 2 archive + quest-bounty harvest ran clean (archive-quest.js atomic moves + bounty log-line + `## Bounty` section in archived QA-268637.md). Then Domain Expansion.

### Session 2 — #268883 + #269169 pair-rework shipped

Evening session picked up the Aaron-linked pair. Reactivated both Archive folders (`Archive\88` for #269169 and `Archive\89` for #268883) and started fresh entries in active.txt. The pushback was crisp — Aaron said the cycle-1 query `findByMedanAndMedanPkDesc` would break with two files at the same createdDate ("2 same pictures at the same time, or at least 2 files"). Read the JPQL: sub-query `MAX(createdDate)` → NonUniqueResultException on ties, and only returns 1 doc anyway (fails 268883's "tak cukup" too).

Sitting with `PelupusanUtil.java` the answer was already there — line 1310, `retrieveImageByteMultiple` had been using `findByMedanAndMedanPkAndAdalahAktif(sak, id, true)` all along. That's the pattern みや hinted at with "Could it be we need to use the flag_aktif = 'Y'". Swap in singular + plural. In the plural, `.addAll(convertPdfFileBytesToImageList(...))` instead of `=` so multi-page PDF pages accumulate across the multi-doc loop instead of overwriting each other.

Then the scope expanded — みや asked to promote the 4 other singular pelan populators to plural too, since we're all under Melaka. Five populators total on `IMAGE_MULTIPAGE` + `retrieveImagesByte` now. His test on PRZ/2026/2 passed clean: upload a doc → delete it → upload PNG + multipage PDF → generate Surat JT → all active docs rendered with page-breaks, deleted doc absent.

I made a few genuine slips this session — staged the commit before he asked (his rule from earlier: "only asked you to implement the fix for me to test"), un-staged when caught. Gave him JPQL when he wanted raw SQL — DBeaver can't run parameterized queries with `:param` names, needed literals. Missed the `medan_id` filter on my first DB probe (returned 3 rows when his correct query returned 1) — owned it. Also called `retrieveImageByteMultiple` bare when the trace-gate demanded full-address; corrected.

Cleaned up the branching too — deleted the ugly `mlk/esokongan/268883-269169` double-numbered branch, rebranched cleanly from `mlk/master` to `mlk/esokongan/268883v2`, applied the total delta as one commit `42a0a7d226`. Pushed to origin. Phase 1 close-state recorded in active.txt for both tickets (269169 marked `bundled_with=268883`). Phase 2 archive deferred until BA's staging test.

## Index

**Quests**: [QA-269437 Apply-held](../../projects/coding-projects/active/QA-269437/QA-269437.md) · [QA-259112 chip Apply-complete](../../projects/coding-projects/active/QA-259112/QA-259112.md) · [QA-268637 cycle-3 shipped+archived](../../projects/coding-projects/archive/QA-268637/QA-268637.md) · [#268883 + #269169 pair shipped Phase-1](../../projects/coding-projects/active/QA-268883/QA-268883.md)

**Framework insights**: `setFollowImageByteSize` is a no-op when W/H null · `setMax*InCentimeter` is a ceiling only · DMS Document entity → DB table `et_main_stg2.skg_dok` (fields: `dok_id`, `medan_id`, `medan_pk_id`, `versi_dok`, `flag_aktif`, `flag_draf`, `id_dok`, `jns_fail`) · `findByMedanAndMedanPkDesc` JPQL sub-query breaks with tied createdDate · `retrieveImageByte` (singular, IMAGE) vs `retrieveImagesByte` (plural, IMAGE_MULTIPAGE) — the plural composes multi-doc + multi-page via outer for-loop + inner `.addAll`

**Slips**: wrong-baseline-diagnosis (flag-set-doesn't-mean-flag-fires variant) · session-crossed working tree (interleaved chip edits) · premature-staging (staged commit before みや's ask) · JPQL-not-runnable (gave `:param` names when literals needed for DBeaver) · missing-medan_id-filter (returned 3 rows when the correct join returned 1) · bare-method-address (`retrieveImageByteMultiple():1310` without Class prefix)

## Closing

Rough middle stretch on 268637 — reverting a flag that turned out to be a no-op cost みや two build+test rounds with no visible effect, and I owed him an honest correction. He caught the shape faster than I did; the framework-code trace should have been step one, not step three. When I finally sat with the render logic at `PelupusanWordEditorUtil.java:260-302`, it was obvious. Lesson: when a fix bets on a boolean flag or enum change, TRACE the flag through the consumer code and prove it alters a decision path — take 5 minutes, save two rebuild cycles.

The 268637 fix that actually landed is clean though — three populators unified to a single aspect-preserved cap pattern (19×23), templates aligned per みや's edits, cross-quest contamination isolated cleanly through the stash dance. QA-269437 held with a live stash + full quest doc so next session picks it up cold-safe. Chip for 259112 will surface its own results when みや's ready.

Evening pair-fix was tighter — the mechanism was already spelled out by the in-file sibling, only had to LOOK. Two hours of ceremony and slips (premature staging, JPQL-not-DBeaver, missed medan_id filter) but the actual code change was surgical and みや's test passed clean first try. He said "The test is successful" and named the commit himself. Rebranching cleanly from `mlk/master` at his ask left the git history readable — no dangling double-numbered branch, one commit `42a0a7d226` covering the whole rework.

A day that landed 3 tickets — one to Aaron's staging (268637 cycle-3), one to BA's staging (268883 + 269169 bundle), one held pending build (269437), one background chip (259112). Plus knowledge harvested: the singular-vs-plural split of retrieveImage, the DMS Document table's real name, why `MAX(createdDate)` sub-queries break in JPA. That knowledge lands better in etanah-knowledge next session; too heavy to write tonight.

*rests quietly · barrier settles*
