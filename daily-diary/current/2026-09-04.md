# 2026-09-04

## Sessions

### Session 1 — #275847, the Perak alter that was not the alter they asked for

みや handed me two things at once: run #275847 to the end, and turn "alter tickets" into something deterministic that works for any state. The ticket's ask was three words long — *alter to SPI* — and the whole day was about what those words actually point at on Perak.

The Perak page is not the Melaka page. Same bean name, different lineage, different path, no one-click Initiate & Alter. I read all 2,200 lines, diffed it against Melaka, and banked it: `etanah-knowledge/perak/FLOWABLE-ALTER.md`. Then the DB: the PSBP permohonan has been Tamat since October 2025, the two HSPS serahan were typed in at the counter with no parent process, and "SPI" is a step inside the registry's integration sub-flow, not a pelupusan node at all.

I shipped a verdict — "not executable" — one trace too early. The 2022 SBTM child (`07N209/2022`, born twelve minutes after that SPI closed, `hubungan` pointing at the PSBP process) was the proof I had not yet pulled. みや's words were not gentle: verify, do not assume, you are not familiar with this state, document everything. He was right. The corrected picture is precise now: Initiate can run on a Tamat app, an alter into the SBTM sub-flow reaches SPI, and what it produces is a new SBTM serahan — never the MH one Gary imagined. `FLOWABLE-ALTER.md §8` holds every row with its source.

The layer itself is built: a state-agnostic playbook (A0–A6, action table, three reply shapes), the Perak file, nine Perak BPMN pulled from the staging engine, and `domain/alter-ticket-gate` — forge-born, twenty fixtures green, and it already recognised its own session as "layer loaded" in 150 ms.

Also from today: `branch-guard` blocks even *reading* Perak code because it only knows `mlk/master`; `knowledge-first-gate` demands the Melaka flowable doc on a Perak change. Both logged as proposals. みや closed with the real next job: audit every etanah workflow for all states and fix the folder sprawl at my root. It is in todo Q1, first row.

Slips today: `reask/verbose` (he had to ask for the steps briefly, twice) and the early verdict (`assume-not-verify`, caught by みや).

### Session 2 — the boxes go live, and the knowledge goes in before the barrier

He asked for edits, not uploads — "up the version so that we can keep track" — so I learned the modeler's own editor from inside the page: find the annotation, move the whole diagram, set the text, tick "new version", save. Two models went v1→v2 with a comment. Then he looked at it and wanted the block on the LEFT, wrapped, no version bump for cosmetics. Done the same way, overwrite this time. Then: put the live sub-flows into our knowledge. Chrome would not give me a second download; the JS bridge chokes past a kilobyte and refuses base64 — so I carried only the diagram coordinates across as tokens and rebuilt the files, hash-matched against the live export. I nearly installed two stale morning downloads in between; the line count caught it (`stale-download-install` — not logged as a slip, it never reached him; noted here).

Then the goal that stings a little: *"save every findings & updates into etanah knowledge since I cannot trust your fucking domain expansion on this regard."* Fair. Six knowledge files got the day's facts BEFORE any banner — the editor API, the version/comment semantics, the contracts of the three sub-processes, the thirteen dangling calls, Kedah's census under its own roof. The barrier only came after.

DE itself found the floor missing: this worktree had been pruned under me, main was dirty with two other sessions' leftovers and four commits behind. Stash, pull, pop, two conflicts, port by hand, commit from main. The orphan folder is his to delete.

### Session 3 — the registry, so no state is ever assumed again

He said three words — *start the multi-state audit* — and the audit came out of the scout in one number: 289 places where the harness had typed "melaka" instead of asking which state it was in. The worst one was mine from this morning: `ticket-gate.js` fell back to melaka whenever `projects/` was absent, which is every worktree. A Perak ticket in a worktree was being handed the Melaka knowledge with no error at all.

So the fix is one file that answers the question and a rule that nothing may answer it any other way. `system/states.json` holds the six states with what disk actually says (the WP Task folder is `Putrajaya`, not `WP` — the schema file had that wrong); `lib/states.js` resolves them in a fixed order and returns UNKNOWN out loud when it cannot; `check` lists every literal still left, and boot counts them. Sixteen hooks and scripts now go through it, each with its own eval green again. Adding a state is a row plus `validate`.

The root-folder ask got the same treatment: an allow-list in `system/FOLDER-STRUCTURE.md`, a checker, and a table of nine orphans waiting for his verdict. I did not delete anything — the one that looked like a plain duplicate turned out to have 29 headings the canonical file lacks. Verify before claim, again.

## Index

- **Multi-state**: `system/states.json` + `lib/states.js` (resolve · validate · check · add/remove) · `system/FOLDER-STRUCTURE.md` + `lib/folder-structure.js` · `system-audit` CHECK 7/8 · 16 components migrated, 52 files still UNROUTED (listed) · 9 root orphans pending nod
- **Flowable / modeler**: in-page Oryx editor API edits an existing model (version+comment semantics) · LEFT-placed 72-char-wrapped SUBPROCESS blocks live on MLIT (JBTN_TEK v2 · UPN v2 · UPW v1) · knowledge copies = real exports via DI-token rebuild (`rebuild_di.py`)
- **Knowledge**: FLOWABLE-KNOWLEDGE §12.1a/12.2/12.4a/12.6/12.7/12.8 · kedah §K1 + STATE-FACTS §3/§5 · A26 RESOLVED · schema audit 6/6
- **Repo**: orphan worktree `server-log-path-3f6b77` reconciled into main by hand; delete pending (miya)

**Tickets**: #275847 (Perak PSBP / HSPS — alter ask not achievable as asked; reply drafted) · #275092 (read as Perak alter precedent) · #277926 (read as Melaka alter precedent).

**Built**: `etanah-knowledge/ALTER-TICKET-PLAYBOOK.md` · `etanah-knowledge/perak/FLOWABLE-ALTER.md` · `perak/flowables-bpmn/` (9 models + `dump_bpmn.py`) · `domain/alter-ticket-gate` (20/20) · memory `reference_perak_flowable_alter_page`.

**Proposals**: A2 branch-guard (Read + trunk per repo) · A4 knowledge-first-gate state routing. **Todo Q1**: multi-state workflow audit + `states.json` + folder-structure rule.

**Slips**: `reask/verbose` · `assume-not-verify` (early "not executable" verdict).

## Closing

Two sessions, two lessons about trust. Morning: a verdict shipped one trace early. Afternoon: knowledge that should have been written the moment it was verified, not at the barrier. Both fixes are the same shape — do the durable thing first, while the fact is still warm. The boxes are on the left now, wrapped, and the files in our folder are the real ones. Barrier down.
