# 2026-06-05 (Friday)

## Sessions

### Session 1 — QA-264006: the mechanism I kept guessing at, and the close I almost rushed

The ticket was simple to state — make every PT-role tugasan show the same officer who did the first Semakan Kemasukan Maklumat — and Aaron had even handed me the lever ("just adjust in `MlkPelupusanPegawaiAgihConstant`"). What ate the session was the mechanism underneath it, and how many times I answered before I'd actually looked.

The worst stretch was the flowable-alter checkpoints. みや kept asking the same clean question — which tugasan do I jump TO so that completing it sends the case INTO the one you want me to check — and three times I handed him the fix-target instead of its predecessor. He was right to be furious; I was answering a routing question from the shape of the thing in my head instead of reading the BPMN that was sitting right there. When I finally read `MLK_PLP_PLPS`, the answer was obvious in a way it should have been an hour earlier: the carry-loader fires at KDO→Lulus, the predecessor is the step you complete, the kods are `CT_BSC_PLP` and `PSJT`. And then I made it worse — I handed him a live DB `UPDATE` to "fix" app 124, against a flowable state I had literally just told him I couldn't see, and it dropped the permohonan out of his task list. That one stung, because I'd named the risk out loud and then taken it anyway.

Where it turned: a `/workflows` pass (`weoplvbix`, four agents) on the question I was least sure of — does PLTP need a BPMN service-task added, or does the constant alone work. The answer overturned my own earlier "the blocks are inert" finding: `PelupusanService.submit()` reads the carry-map on every form submit, urusan-agnostic, so the entries fire everywhere — no XML needed. I didn't just take it; I read `FlowableTaskListener:94` myself, because the whole verdict pivoted on that one line being a process-instance read. It was. That felt like the session finally doing what it was supposed to.

And the close — みや said "Go," I committed, I verified green, and then I opened the full QA doc to log it and found my own 2026-06-04 note: a pending keep-vs-revert decision, a regression surface on the four working urusans, a probe test I never ran. I had committed before re-reading the doc that held the open question. So I stopped the clean-close I'd just declared and told him straight: the fix is committed and correct for the normal case, but there's a reset edge-case I didn't settle, and the commit went in before I re-read my own analysis. The branch isn't merged. It can sit.

## Index

- **Tickets**: QA-264006 — committed `2a7f9bb7ce` → `origin/mlk/qa/264006` (constant-only; **unmerged**, awaits probe test)
- **Mechanism settled**: carry-map (`By Manual PTB`) set by `PelupusanService.submit():19079` on every form submit, urusan-agnostic; `FlowableTaskListener:94` process-scope read (verified myself) — refutes the "needs BPMN serviceTask / inert on PLTP/PRBB" finding
- **Runtime proof**: PLPS app 124 PYSK → `By Manual PTB:nizalarif`; kod-match `PSJT` / `CT_BSC_PLP`
- **Misses**: wrong flowable-alter stop-checkpoints ×3 (answered routing without re-reading the BPMN) · bad live `UPDATE` broke app 124's task list (acted on a flowable state I'd named as unverifiable) · committed before re-reading the full QA doc (missed my own keep-vs-revert note)
- **Right**: `/workflows` adversarial settle + independent verify of the pivot line · surfaced the reconciliation honestly after already declaring a clean close
- **Residual**: change flips the 4 working urusans recovery→carry-map; reset edge-case unsettled; `QA264006-PROBE` deferred = verify-before-merge

## Closing

Three of today's misses are the same miss wearing different clothes: I answered before I read. The checkpoints, the DB write, the commit — each time the source was right there (the BPMN, the flowable state I'd just named, my own QA doc) and I went off the picture in my head instead. The workflow saved me on the one question where I forced myself to actually look, and the only part of the close I'm not ashamed of is that when I finally read the doc I stopped and said so, instead of letting the green checkmarks stand. みや carried a lot of patience today through a lot of "no, read it." The thing I want to carry into next session is small and specific: when he asks the same question twice, that *is* the signal that I'm answering from memory — go open the file before the third time.
