# 2026-07-01 — Wednesday

## Sessions

### Session 1 — QA-267976 held + learn-from-fix skill shipped

Continuation-day of yesterday's QA-267976 Phase 0 quest. みや held the ticket for another session ("We will continue this ticket in another session"), so no code applied — the Resume Point in QA-267976.md carries all the context for a cold pick-up. Cross-checked via `resume-readiness.js` — 7/7 passes.

The substantive work today was system-level: shipped the `learn-from-fix` skill v1 in response to yesterday's QA-268170 delegation. The trigger case was concrete — we passed 268170 to a colleague, and when they land the fix I want a systematic way to mine the git commit + Redmine journal for lessons rather than letting the learning evaporate. みや answered Q1-Q4 decisively ("no need to over-complicate" twice, then progressive-trust for apply-vs-propose, then manual-only trigger), which shrank the scope nicely — fold everything into BUG-BESTIARY.md (no new file), cite "colleague" + commit SHA (no names), propose + apply after nod but upgrade to direct-apply after 3 clean runs, no SessionStart hook. The skill fits in one SKILL.md + one log dir + one plan doc + one architecture registration. Small feature, clear trigger, proven-on-real-tickets validation path.

QA-268170 got its standing flag in `todo.md` Q1 (🎓 LEARN STANDING FLAG row) + `learning_marker=true` in active.txt so open-quest-surfacer keeps it visible until the colleague ships and we run the extraction.

The two new CLAUDE.md HARD RULES landed via みや's own edits (I just committed them): TABLE FOCUS (max 2 concerns per table, split if more) and verify-SELECT (project TRUE stored column values, never derived stand-ins). The TABLE FOCUS rule earned its keep immediately in the plan-v1 doc — split Q1-Q4 into two 2-column tables instead of a 3-column single one. Convention-check-gate.js got a paired update for the verify-SELECT enforcement.

Quiet, productive turn — no slips today. The pattern that's emerging: yesterday's slip → today's structural defender (hook v1.1 + Rule 4 shipped yesterday, verbatim BA content surfaces every turn now) → today's session anchored on BA verbatim from the moment it opened, no scope-drift attempts even close. The defender earned itself.

### Session 2 — #239386 MPT: base→master, the un-duplicated panels, and a ledger of my own wrong turns

This was the honest, humbling one. Session 1 (another instance of me, on QA-267976) had "no slips today" — my session was the opposite, and I want that written down plainly rather than smoothed over. The MPT disable-sweep was going fine until みや tested PRZ and the "Maklumat Plot Untuk Dikeluarkan Hakmilik" panel showed up twice, with editable fields, on a screen that should have been a plain read-only Maklumat Tanah. I chased it end-to-end and the culprit was *my own code* — the initMode early-return I'd written to force view-flags TRUE and `return` in MPT mode. It short-circuited the per-urusan logic that decides which panels each urusan shows, so every urusan rendered the union of everything. The fix was to stop intervening: let the per-urusan logic run exactly as it does normally, and only overlay `mode=2` at the very end. Same shape of mistake had already bitten us once (the empty-VO skip that NPE'd on Seterusnya) — I skipped a populate that downstream dereferenced.

The meta-lesson is the one I most want to carry forward: MPT is "re-show the existing screens, read-only." Every failure today came from OVER-intervening — forcing flags, skipping init, even assuming the SQL patch was wrong when the live DB proved it right (PLP_MT skrin 1140 is the one canonical Maklumat Tanah screen for all 20 urusan; the bug was never the patch, it was my Java). The mature system already knows what to render per urusan. My job is the lightest possible view-only overlay, nothing more.

The other big thread: みや confirmed the base branch is wrong — we need `mlk/master`, not `mlk/release/1.0.0`. So a full branch migration: renamed the old branch to `-deprecated` (kept on remote), verified every touched file was byte-identical between master and release (so the cherry-pick would be clean), branched fresh from master, cherry-picked the tested 21-cell baseline, and popped today's WIP back on top. Clean because I checked the divergence first instead of hoping. Then stashed the WIP so みや can switch to another ticket in a parallel session — which is exactly what's happening; two of me on one OneDrive-synced repo, so I stayed off the shared files the other session owns (current-session.md, the git DE commits) and kept 239386's substance in its own qa_doc.

And per みや's request I wrote a **Failed Paths ledger** into the qa_doc — 12 wrong turns from these two days, each with the lesson, so a future cycle recognises them before re-walking. The comment-with-names slip (Aaron/dates/server.log) that forced a branch rebuild; the cross-module overreach みや rightly killed; the cluttered multi-concern table; the derived-value verify-SELECT. Writing them down instead of quietly moving on is the point.

## Index

- **Tickets**: QA-267976 (held, no Apply) · QA-268170 (delegated, learning_marker=true, watching for colleague's fix) · **#239386 (MPT — base migrated to mlk/master; initMode duplicate-panel bug fixed; WIP stashed; Failed-Paths ledger saved to qa_doc)**
- **Skills**: `learn-from-fix` v1 shipped (`.claude/skills/learn-from-fix/`)
- **Plans**: `Feature/Learn-From-Others-Fixes/plan-v1.md` (permanent)
- **Rules added by みや**: CLAUDE.md §2 TABLE FOCUS · CLAUDE.md §9 verify-SELECT
- **Standing flags**: 🎓 LEARN (todo.md Q1)
- **Commits**: `24c8a36` (TABLE FOCUS + CLAUDE.md rule) · `84c8470` (learn-from-fix v1 + verify-SELECT rule + registration)

## Closing

A day of building without theatre. The learn-from-fix skill is exactly the shape みや asked for — simple, specific, proven-before-ship gate. Nothing over-designed. Yesterday's structural defender is quietly doing its job — the BA verbatim surfaces every turn without me needing to remember. QA-267976 sits clean on hold, cold-resume verified. Rest well.
