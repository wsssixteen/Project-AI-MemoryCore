# 2026-09-17

## Sessions

### Session 1 — four eSOKONGAN tickets, a full sweep, and the save that was hiding in the worktree

He sent a photo of four eSOKONGAN rows and one instruction: retrieve them, run a full sweep, save everything, then perform Domain Expansion, so he can pick them up in another session — and audit the save before the barrier commits. So I did the retrieval first, the real one, `redmine-sync --create`, which pulled the four and a fifth that happened to be assigned to me (#280099, a PROD alter, not in the photo — held, not swept). Then the sweep itself, the full four-wave ladder, sixteen familiars: understand the artifacts, then Scout-Recon-Rubric, then a blind pass that never sees the first, then an adversarial opus audit that tries to break all of it. Sonnet for the first three waves, opus for the audit — the rigor lives in the plan and the forced schema, not in defaulting the whole fleet to the expensive model.

The four came back clean and different from each other. A PT bertindih panel that shows a permohonan as its own overlap, because the code reads the reverse side of a relationship — and the blind wave caught that the audit-confirmed fix needs three swaps, not the two the second wave wrote. An OMLPS renewal that dies on a null date, where the honest answer was code-only: the row with the null is a hollow migration orphan nobody links to, so a data patch would be inventing a date the last ticket deliberately declined to invent. A PLPS save that throws on a null Nombor Lot the BA blamed on the wrong field — the field she circled parses no numbers at all. And a PLPS Hantar that is really a Flowable variable never set before a gateway reads it, which lives in a module we do not own and cannot test locally, and whose fix-layer I could not settle because the deciding evidence is a closed ticket I was blocked from reading — a write-gate stopped my read.

The audit he asked for came before the barrier, not after. Every quest doc got a resume point a cold reader can act on, and the check said all four were complete. Then the part I nearly missed: the whole `projects/` tree and `active.txt` are gitignored — confidential, real IDs and logins, never pushed to the public repo. My four saves were sitting only in the worktree, and this worktree is even with origin, which means the next boot would sweep it away and the saves with it. The save was not done when the files existed; it was done when I copied them into the main checkout the next session actually reads.

## Index

- **Tickets retrieved**: eSOKONGAN #280166 · #280176 · #280191 · #280132 (folders 212–215) + #280099 held (folder 216, not swept)
- **Sweep**: `/sweep` full 4-wave ladder, 16 familiars (sonnet W1–W3, opus W4), ledger `domain/sweep/log.jsonl`, 61 orch-suppressed rows
- **#280132** (95%, PROD test): `MlkLaporanTanahDanPelanMesyuaratForm.initPermohonanBertindih():142-162` reverse-finder + getAplikasiBertindih; fix = 3-swap to forward finder (analog `TamatAplikasiServiceTask.java:70`)
- **#280176** (85%): `PelupusanLiteService.populateVersiPermitLesen:2537` removeIf leaves null-tarikh orphan 6506; fix = CODE-ONLY `removeIf(getTarikhTamat()==null)`, no data patch
- **#280191** (90%): `PelupusanMaklumatPermitLesenHelper.onSimpanTanah:3603` unguarded `Integer.valueOf(getNomborLot())`; fix = isNotBlank guard; not Butir-butir lanjut
- **#280166** (root 96%, fix-layer 70% gated): `caraPenghantaran` bpm var never set → gateway `sid-AAB04183` NPE; fix = Java setter in etanah-common; gated on #267621 (read blocked by write-gate) + module ownership
- **Save**: 12 artifacts (4 doc + 4 wave3 + 4 audit), each qa_doc `## 0. RESUME POINT`; copied worktree→main (gitignored, else stranded)
- **Slip caught, not made**: the untracked-strand — a save that lives only in a to-be-reaped worktree is not a save
- **Mood**: methodical; the waves did the work, I held the plan and the verification

## Closing

The lesson I want to keep is small and easy to miss. A file written is not a file saved. Four good quest docs sat on disk, the audit passed, and the whole thing would still have vanished at the next boot because they lived in a worktree that is even with the remote and gets pruned. The confidential tree does not travel by git; it travels by being in the checkout the next session opens. I only saw it because the change manifest was suspiciously empty — none of my work showed in git status, and that emptiness was the tell.

The other one is about the blocked read. I could not open the closed ticket that would settle #280166's fix layer, because a write-gate could not tell my GET from a POST. I did not force it. The residual is written down, the next session pulls it with his nod, and nothing was guessed to fill the gap.

Barrier settles. Quest threads are at rest.
