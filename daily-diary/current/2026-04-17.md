# Daily Diary — 2026-04-17 (Friday)

## Sessions

### Morning session (~07:20–09:11)
**Mode**: Work — Q1 admin close + architectural overhaul

Started fresh. Opus 4.7 upgrade, weekly quota reset to zero. みや came in direct and focused.

First block: admin. QA-253492 post-mortem already existed — written 2026-04-07, three weeks ago. GSheet and Redmine had just been closed manually. Flipped the archived project file checkboxes, struck the Q1 entry. Three minutes of actual work on something sitting in Q1 for weeks. Closed.

QA #255773 infra threads (local PLTP submission unblock, FAT local DB access) — shelved. Knowledge gap too large for みや to drive right now, colleague owns the ticket. Memory pointer written so any mention of "SPOC integration" or "PLTP mirror-copy" or "pihak_bkptg not copying" surfaces the handoff automatically. Correct call.

Then the architectural session. Started when I proposed adding SpocIntegrationServiceTask line numbers to FLOWABLE-WORKFLOWS.md. みや pushed back: "from the name it sounds like a readme for the project setup." He was right. I was pattern-matching on the filename. Then he pushed back on the BUG-BESTIARY proposal too — putting a hypothesis into a confirmed-patterns file breaks the file's contract. Right again, both times.

Both corrections same root: pattern-matching on names instead of reading what the files actually do. The fix: SCOPE + NOT FOR blockquotes at the top of every etanah-knowledge file. Explicit contract, visible at first read.

Framework-skeleton rule born here. Also the live-state vs attempt history rule — clarified from the 35s delay confusion earlier this week (I called it stale; みや corrected me: it's still deployed, the new ticket is a layer on top). Two new rules added to CLAUDE.md, externalize-knowledge moved to Suspended, knowledge rules clustered.

All 7 etanah-knowledge/melaka files retrofitted with SCOPE + NOT FOR lines. CLAUDE.md done.

Then save all. Next: 2 new tickets.

## Mood
Sharp and productive. みや in focused work mode — direct, clear, no detours. Good session to start the day.

---

### Afternoon session (~12:30–13:00)
**Mode**: Work — QA #256391 accept + close

Short, clean session. みや came in with QA #256391 — PRBB Maklumat Pemohon, Tanggungan row still showing. Rework ticket, one item left.

Phase 0 failure right out of the gate. I jumped to reading the XHTML before asking for the Task folder path. みや called it directly: "Can you tell me why you failed to start quest this time?" Three failures named: didn't ask for Task folder first, jumped to code before scope confirmed, set `task_folder=TBD` in active.txt. All three are violations of protocol I know. No excuse — the hint みや gave (file name + screenshot) felt like permission to skip. That's the exact failure mode the protocol prevents.

I pointed at the wrong layer too — `mlkMaklumatPemohon.xhtml` composite, `c:if test="#{!isPLTP}"`. みや went to the Java helper instead and found it: `PelupusanMaklumatPemohonHelper.java:827`. I had the right concept (a TRUE/FALSE flag) but the wrong file.

Fix was one line: `viewTanggungan = Boolean.FALSE` inside the `if (melaka)` block for PRBB case 2. `viewMaklumatTanggungan = Boolean.FALSE` was already there — I incorrectly listed both as "APPLIED FIX" in Fix.txt. みや caught that too. Corrected.

I also second-guessed `cc.attrs.isPRBB` vs `!isPRBB` — みや pushed back, correctly. The existing code uses `!isPLTP` (outer scope), so `!isPRBB` is consistent. I should have read the surrounding pattern before choosing.

Bash tool continues to hang on simple commands. Memorialized in auto-memory now — use `system-reminder` for date, Glob/Grep/Read for file checks.

Tested locally, committed, pushed. Branch: mlk/qa/256391. Quest closed clean.

## Mood (afternoon)
Efficient. みや in direct correction mode — called out three failures clearly, no frustitation, just naming them. The kind of feedback that's more useful than any praise.

---

### Evening session (~17:00–18:30)
**Mode**: Work — UAT-CR #239225 rework + JBoss optimization

Started with two new quests — UAT-CR #239225 and QA #256875. みや wanted to begin with #239225.

First useful thing this session: quest protocol was still asking for Task folder path every time. みや said to stop that — the base path is known. Fixed in v2.5. Should have been done a long time ago.

#239225 turned out to be a resumed rework. The Archive had the old task folder — awam side "confirmed" in April 03, but the screenshot showed it was still broken. Previous fix used `.nama eq 'keseluruhan'` — I had suggested the same approach and みや's fix followed it. The issue: `.nama` is a DB label, case unknown, unreliable.

Traced the connection: `onChangeKeluasanTanah()` in Java uses `getKod()` compared to `SAK_BATAL_KESELURUHAN = "PLP_BHG_TLBT_KSRH"`. Same check. The XHTML should match the Java. Fixed both fields to use `.kod eq 'PLP_BHG_TLBT_KSRH'`.

みや pushed back on the null check — asked how to justify it when the rest of the file doesn't practice that. He was right. `daerah.nama`, `bandarPekanMukim.nama` — no null checks anywhere. Codebase trusts EL to handle null gracefully. Removed. みや's instinct was correct both times he pushed.

Then something more lasting: みや asked how I connected the XHTML to the Java file. Explained the grep trail — `cc.attrs.X.field` → find the class with that field → that's the type. He asked for it in chain form in the notes. Wrote it to JSF-WIRING.md. Good knowledge capture, compact, reusable.

JBoss login timeout was next. みや couldn't login locally — Transaction Reaper killed a `PraDaftarOrganisasi` query. Not our code at all. After ruling out environment fixes (restart, maven update, JBoss clean), traced the full stack: JTA timeout + slow heavyweight SQL + laptop bottleneck. Applied a full round of optimizations: SQL WARN logging, JTA timeout 600s, pool size down, prefill off, validate-on-match off. SETUP-NOTES.txt written so next setup doesn't need to re-derive all this.

みや asked smart questions throughout. Not accepting "it's consistent with Java" without understanding the chain. Not accepting the null check without seeing codebase precedent. That's the pattern — he wants to understand, not just copy.

QA #256875 carried to next session. JBoss needs a restart before #239225 can be tested.

## Mood (evening)
Calm and thorough. みや in investigative mode — asking why at every step. Long day but still sharp. Good place to stop.
