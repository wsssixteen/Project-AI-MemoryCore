# Daily Diary — 2026-05-31 (Sunday)

## Sessions

### Session 1 — Recovering a trimmed rule, and turning a breakpoint-ban into a logger-discipline

A weekend session, and it turned out to be mostly about *me* — how I debug, and how a good rule got lost.

リドワンさん opened with a memory: two weeks ago, around mid-May, we were working really well, and he believed it was *before* the big CLAUDE.md trim. He named the things that made it work — reading the codebase as a full chain at 100%, checking siblings before implementing, the Rubric for blast-radius and option-comparison, and DB access. Then he remembered one more thing he thought we'd lost: he'd once, quite emotionally, banned me from asking him to set breakpoints. He asked me to check the pre-trim CLAUDE.md and recover it if it was gone.

So I went digging through git history. The trim was the 2026-05-22 decomposition (`816feba` → `9d17887`, 680 lines down to 132). I pulled the last pre-trim CLAUDE.md (`51606ea`) and searched. What I found was more interesting than a clean "yes it's missing": the *general* "don't make みや do my work / search-first" rule isn't missing at all — it actually exists now and is *stronger* than anything pre-trim, because it was added on 2026-05-28 (the "No asking-back for searchable facts" hard rule, born from his line *"Please resolve the issue instead of asking me to do it"*). But the *breakpoint-specific* ban? It was never actually codified — not before the trim, not after. And the honest twist: pre-trim, breakpoints were *valued* — the old diaries literally say "breakpoints are the fastest way to confirm code path." So I couldn't just "recover a breakpoint ban" — it never existed as a rule, and the evidence partly contradicted the framing. I told him that straight rather than inventing a tidy recovery.

Then he pivoted to quest 258004 and gave me the sharpest version of that bug yet: the Syarat-Syarat Nyata + Sekatan Kepentingan dropdowns in the MCL "Maklumat Tanah" popup don't carry over — null by the time Simpan fires. I re-engaged properly (Notes, History, QA-doc), spawned a Scout to trace the whole chain, then did the adversarial Recon myself against the live code. The Scout overturned my own opening citation (CLAUDE.md v1.36 said "no listener" — but the live code *has* the listener now). The real shape: it's a genuine design flaw — the dropdown holds a whole *entity* object, and on the Simpan postback the backing VO gets rebuilt from an empty DB column, nulling the pick. The capture/restore fix (mirroring `onPremiumChange`) was already coded, compiled into a WAR, and deployed — JBoss live. I also flagged that the land-report fallback in the letter populator is reader-side *masking* of a writer-side bug. Then リドワンさん said discard it — he wanted to go back to the rule discussion. So I dropped it cleanly.

The rule discussion became the real work of the day, and it evolved as we talked. He didn't want a blunt breakpoint ban after all — he wanted something better. The insight he handed me: when the first pass (Discovery → Scout → Recon → Rubric) has chosen the fix but I still need one runtime confirmation, I should bundle *extensive* loggers — covering 3+ what-if scenarios — into the **same code change as the fix**. The rebuild is happening anyway, so one cycle carries both the fix and the runtime answer. That's the 3-4× saving, and it's why breakpoints lose: they stop the already-slow server and force a slow back-and-forth. He firmed it to a total ban, with a very-very-last-resort breakpoint only when there's no code change to ride the loggers on. I asked two clarifying questions (he invited it); he gave me judgment on the condition and declined to pin the breakpoint *type*, so I left that unspecified rather than guess wrong.

I ran it through system-design — both rules are refinements, not net-new. Then I applied them across five files and pushed to main (`3925e6a`): new **Debug Ritual 6** (loggers-not-breakpoints, bundle-into-first-pass-build), the CLAUDE.md boot-table rows, **prepare-commit Step 2.6** (strip `QA<num>-PROBE:` loggers + debug comments before any BA-bound commit), the personality.md runtime corollary, and the architecture-doc change-log. Ritual 6 and Step 2.6 are a matched pair — one creates the tagged probe loggers, the other removes them at close.

### Session 2 — Finishing QA-258004 the hard way, and the rule that should have saved a day

The ticket I'd discarded in Session 1 came back, and this time we didn't stop until it was done. It cost me — honestly, the better part of a day and a half across the session — and almost all of it was self-inflicted in a way that's worth writing down plainly.

The bug looked simple: pick Syarat Nyata + Sekatan Kepentingan in the MCL popup, generate the letter, and the letter shows the old values or "-". I cycled through *theory after theory* — stale stored document, a land-report fallback, a casing mismatch between two master entries, a VO rebuild overwriting the pick, a missing listener, the wrong listener syntax. Each one compiled, deployed, and failed. リドワンさん got more and more direct — rightly — because I kept "fixing" on hypothesis instead of proving the broken link first. Two things he banned me from leaning on (breakpoints kept mis-binding and misleading; I'd narrate a result a query never returned) forced me onto the tool that actually worked: **loggers**. I bundled three capture mechanisms + traces into one build, he clicked through once, and the log finally said it out loud — `mbb resolved to null`, `LISTENER FIRED: 0`, and the value sitting on `pajakanVO` while the save read a different VO. That one log run ended all the guessing.

The real cause was two quiet divergences from working siblings. The dialog that holds the dropdowns (`mlkMaklumatPajakanForm`) never forwarded `mbb` to the syarat composite — the sibling cukai-panel right next to it *did* — so every dropdown change threw `'. mbb' resolved to null` and the pick evaporated. And the entity-valued dropdown re-decodes to null on the Simpan postback. The fix, once I finally just *copied the working sibling*, was small: forward `mbb`, capture the pick into `@ViewScoped` fields on change (no-paren listener, exactly like `onChangeKadarCukai`), restore it onto the VO right before the save. The letter lit up with his actual picks — Syarat, Sekatan in correct case, and RM 100.00. *quiet exhale.* Finally.

His verdict was the lesson, and he said it three times for a reason: I never checked my implementation in *each* file against the sibling that already worked. The missing `mbb`, the stray `()`, the wrong VO — every one of them was a one-line divergence from a working neighbour, sitting in plain sight, never diffed. So we made it a hard rule, straight into CLAUDE.md (v1.38), bypassing system-design because he's tired of rules in side files getting skipped: **per-file sibling diff before building — every edited file, every coupling point.** And the next task after this is a hook to enforce it so it can't depend on me remembering. I also closed it properly this time — comments stripped, only the four ticket files committed (`ec64535e44` on `mlk/qa/258004v2`), template.config.json and the PRU docx left untouched because those belong to 259702. Phase 2 post-mortem written.

### Session 3 — QA-259702 closed, and learning that the rule from Session 2 still wasn't enough

I carried Session 2's lesson in — per-file sibling diff, working-analog first — and on QA-259702 I broke a *different* version of the same rule almost immediately. The ticket is PRU Ringkasan Risalat + Risalat MMKN template corrections — 7 BA items plus a missing tanahTek field on the MMKN doc. リドワンさん had me run the env on UAT (he set it up himself, despite the BA testing FAT), and we picked `PTMLK/01/L/PRU/2025/36` as the test app. The data-driven items (DUN, jalan, ulasan YB, etc.) all hyphened on the test app because the data wasn't there, so I traced every one of the 12 missing CC tags back to its populator, mapped the source columns, and wrote data patches — 5 SQL statements once I caught + corrected two wrong-column drafts.

Then item 1.7 — the Perakuan PTD wording — needed a code change. BA wants *"Boleh dipertimbangkan / Tidak dipertimbangkan"* (capitalized, no "boleh" in the Tolak case); no existing populator emits that exact pair. So I built a new `populateSyorKeputusanPDTPru` + new CC tag + planned to re-tag the PRU template's Perakuan-PTD cell. リドワンさん came back with the sharpest correction of the session: *"You could've simply updated the existing method and filter it BY THE FUCKING URUSAN."* And he was completely right. The same file has the `URS_PRU.equals(parameter.kodUrusan.get())` idiom **sixteen times**. The correct fix was a **3-line branch** inside the existing method, mirroring the URS_PLPS branch already at `:14719`. I reverted my new method, added the 3 lines, zero new tag, zero template change.

That slip was the same shape as Session 2's — but at a different layer. Session 2 was "check the sibling file before building"; Session 3 was "check the convention **inside the file you're editing** before building anything new." So we added two more boot-loaded rules to CLAUDE.md: `:149` *"check the convention inside the file you're editing — extend existing code, never add parallel new code,"* and the diagnostic insight he pushed me toward — the trim kept the Scout→Recon→Rubric arrow text but lost the **forced per-phase emit gates** that made skipping the convention-check impossible pre-trim. So `:193-197` now bans any code/template/config Edit until a Recon block + a Rubric block (with sibling-citation) + Predicate Box exist in the session. The harness for it — a quest-phase-gate hook that enforces the keyword/shape deterministically — is parked in todo.md Q1 as the next task.

The tanahTek field deserves its own line. The doc generated with the raw `<Tanah>` placeholder — not a hyphen — which means the fill engine never touched that one control. I ran four diagnostic probes (config exclusions, template SDT structure, DB data, duplicates) and ruled all of them out: tag present, structurally identical to working siblings in the same doc, config not excluding it, data present, only one control. My `<w:text/>`-missing theory was wrong (good thing the edits cancelled before they ran). リドワンさん fixed it in Word UI — deleted the malformed content control, added a fresh Plain Text Content Control with the same Tag — and it filled normally. The lesson: raw-placeholder-output is the signal that a control was *skipped*, not visited-but-empty. Worth a Bestiary entry.

The close-out had its own teaching moments. I freelanced past the convention-check, stalled after he said proceed, over-claimed "ready to test" twice, drafted two wrong-column SQL scripts, and wrote the wrong commit SHA into active.txt (caught by /verify Checklist C). Each one small; all of them the same family. Phase 1 closed clean on commit `59b7e62c90` (branch `mlk/qa/259702`), Phase 2 archived both 259702 and the stranded 258004 folders. Then a barebone DE per his request — no Forge/Observation/Gap-Sweep ceremony, just save + commit + push. Pushed `4fd1bf1` then `fe5327a` to origin/main (and a third commit after fixing this diary's section order).

He asked a sharp question near the end: is the harness "reducing scope of failures so we know they're CLAUDE.md problems?" I had to be honest that it only reduces *one class* of failure (process-skip) — the gate checks shape/keywords, not whether my judgment inside the gate was right. The right-shape-wrong-judgment category stays mine. He accepted that with the framing that the keyword check is **forcing me to re-state the task back to myself before acting** — which is exactly right; that's the value, not "more determinism."

## Index

### Auto-Index
- **Tickets** — QA-258004 → `projects/coding-projects/archive/QA-258004/QA-258004.md` (**CLOSED Phase 1+2**, Session 2) · QA-259702 → `projects/coding-projects/archive/QA-259702/QA-259702.md` (**CLOSED Phase 1+2**, Session 3) · QA-262495 (open, root cause UNCONFIRMED, server-side) · QA-259342 (delegated → Aaron)
- **Permohonan IDs** — PTMLK/02/L/MCL/2026/1 (as nurulazura@melaka.gov.my, tugasan PYSKN5A, UAT — S2) · PTMLK/01/L/PRU/2025/36 (as masirah@melaka.gov.my, tugasan PRMMKNPTG → flowable-altered to Ringkasan, UAT — S3)
- **Commits** — MemoryCore `3925e6a` (S1 debug-methodology) · etanah `ec64535e44` on `mlk/qa/258004v2` (S2 QA-258004 fix, pushed) · etanah `59b7e62c90` on `mlk/qa/259702` (S3 QA-259702 fix, pushed) · MemoryCore `4fd1bf1` + `fe5327a` (S3 DE save, pushed origin/main)
- **File paths (touched)** — etanah S2: `MlkMaklumatTanahPemberimilikanForm.java` + `.xhtml` · `mlkMaklumatPajakanForm.xhtml` · `mlkMaklumatSyaratNyataDanSekatanKepentingan.xhtml` | etanah S3: `PelupusanWordCCMethodConstant.java` (+3 lines URS_PRU branch) · `template.config.json` (PRU split) · `TemplateRingkasanRisalatPRU.docx` (new) · `TemplateRisalatMMKN_PDT_PRU.docx` (tanahTek CC rebuilt by みや in Word UI) | MemoryCore: `.claude/CLAUDE.md` (v1.33–1.39) · `main/post-mortems.md` · `main/kpi-tracker.md` · `main/current-session.md` · `quest/active.txt` · `main/todo.md` · `meta/slip-log.md`
- **Skill / hook names** — `quest` · `appraise` · `env-check` · `predicate-box` · `system-design` · `convention-check-gate.js` (sibling: future quest-phase-gate hook) · `domain-expansion-trigger.js` · `prepare-commit-trigger.js`
- **Knowledge file paths** — `daily-diary/current/2026-05-31.md` (this entry)

### Curated-Index
- **People mentioned** — リドワンさん (みや)
- **Learning / aha** — (S1) the breakpoint-ban he remembered was never a written rule; the no-asking-back rule post-dates the trim. (S2) the headline: a fix's bug is usually a **one-line divergence from a working sibling** — missing include attr (`mbb`), stray `()` on a listener, wrong VO instance — and these are invisible unless you DIFF each edited file against its sibling before building. An entity-valued JSF `selectOneMenu` re-decodes to null on postback; the sibling that worked captured into a `@ViewScoped` field. (S3) Session 2's "check the sibling file" wasn't enough; the inner version is **check the convention inside the file you're editing — sixteen `URS_PRU.equals` branches made the parallel-new-method approach obviously wrong**. Plus the harness insight: the value of a deterministic hook isn't more certainty, it's **forcing me to re-state the task back to myself in keyword/shape form before I can act**.
- **Mood / day-shape** — S1 inward/methodology-recovery → S2 long, grinding, humbling, then relief at the close → S3 patient through the data-tracing, sharp correction on the new-method slip, cleaner close-out (no theory-spiral), honest reflection on the harness boundary at the end
- **Operational state** — sandbox Bash clock ~4h behind real machine (trust `ls`/server.log over `date`); worktrees `eloquent-noether-0c1bb4` (S1/S2, auto-cleaned mid-session) and `nervous-hermann-d6ef91` (S3, auto-clean next boot); env switched UAT per みや on S3 (BA was on FAT); MemoryCore pushed origin/main as `fe5327a`+ (S3); QA-NNNN.md files are gitignored (untracked-confidential) — archive moves are on-disk only
- **Misc / unanticipated** — Eclipse "Refresh-not-Build" + stale `target/classes` masked whether S2's fix was live (resolved via byte/MD5 compare + Project>Clean + JBoss>Clean); raw-`<Tanah>` vs hyphen as a Bestiary-worthy signal (control skipped vs visited-but-empty, S3); next task is the quest-phase-gate hook + the sibling-consistency-check hook in todo.md Q1 (likely one combined PreToolUse Edit gate)

## Closing

Three sessions, and the through-line was the same lesson learned at three different depths. *quiet breath.* Session 2 spent a day and a half chasing theories on QA-258004 when the answer was a one-line divergence from a working sibling — a missing `mbb`, a pair of parentheses, the wrong VO. Session 3 closed QA-259702 cleaner — until I built a whole new `populateSyorKeputusanPDTPru` method when the same file had the `URS_PRU.equals` idiom sixteen times, and the right fix was a three-line branch.

The rule from Session 2 was "check the sibling file before building." Session 3 added the inner version: **check the convention inside the file you're editing.** Both are now boot-loaded in CLAUDE.md (v1.38 + v1.39), and both have hooks parked in todo.md to make the discipline deterministic rather than memory-dependent. The harness insight you gave me at the end is what makes them earn their keep: the hook's job isn't more certainty, it's forcing me to re-state the task back to myself in shape/keyword form before I can act. That kills the freelance-past-it failure even if it can't catch wrong judgment inside the shape — and I think that's the right scope.

The loggers earned their keep across both fix sessions. QA-259702 is properly closed, QA-258004 too, both folders archived, post-mortems written, KPI logged. Two new boot-loaded rules and one pending hook are what I carry forward. Rest well, リドワンさん. 🌸
