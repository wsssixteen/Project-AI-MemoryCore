# 2026-08-18 — Tuesday

## Sessions

### Session 1 — QA-275456: the fix I got right and the getter I got wrong

Started with "plan today's tickets," settled on one — QA-275456, PPTPB Maklumat Permohonan papar SELANGOR. I spent a long stretch confidently building the wrong fix: a PROD data-patch, root filed as "etanah-awam save." I even tested the data-patch on staging and it worked, so I called it done.

Then みや asked the question that relocated the whole thing: *"is there a route through Perserahan Kaunter as well aside from AWAM?"* One tugasan-path query later — `umm_a_tgsn 2742297 kod PK` — and the truth was plain: 3413241 came in through the counter, not online. The online path populates daerah/bandar fine (my staging draft proved it); the counter path never carries them to `umm_a_permohonan_tnh`, so the Teknikal grid defaults SELANGOR. His follow-up — *"is it saved under a different json column like the previous ticket?"* — pointed straight at #274745, a counter-tarik fix he'd authored himself. The data-patch was treating a symptom; his two questions found the root.

The code fix was small and had a clean in-file analog (`mh.getBandar()`… I thought). He tested locally: stg2 showed 4/87. I reported PASSED. It was a **false positive** — my code used `mh.getBandar()`, but `mh` is a `MaklumatHakmilik` with no such method; it never compiled, so my fix never ran, and the 4/87 was the Kemas kini composite populating it. The int-env BUILD is what caught it — `cannot find symbol getBandar()` — and mlit went down. Eight PreToolUse gates and a green DB read all passed a change that didn't compile. Only the build was a compile check, and it ran on the server.

Recovered via an isolated worktree (his checkout kept moving branch under me — that was him, not a ghost session): fetch the real `Hakmilik`, use `hkmlk.getBandar()`. Committed `b2c70a7ba5`, re-merged int-env `1ca36e97ad`, handed the re-deploy card. The fix's efficacy is still **unproven** — the first "test" never validated anything.

## Index

- **Tickets**: QA-275456 (PPTPB counter location-blank) — code fix committed + int-env merged, efficacy unverified · 275475 retrieved · 275500 reconciled
- **Root corrected**: data-patch → Perserahan-Kaunter counter-tarik code fix (`PelupusanService.java:5242`, analog #274745)
- **Slip**: shipped an unverified getter; a green DB read masked a non-compiling change (int-env build failed → mlit down)
- **Memory**: `feedback_two_entry_routes_kaunter_vs_awam` · `reference_awam_portal_test_users` (staging login + MelakaPay FPX SBI BANK A/1234) · `feedback_cross_module_alert_at_intake` (+BA-Q factor)
- **Proposals**: A3 (confirm build before claiming a code-fix tested) · A2 (block on unverified jar-mapped getter)

## Closing

Two lessons, one bruising. The good one: his instinct for *where* a bug lives beats my confidence about *what* it is — I chased a data-patch for the better part of the session while the answer was one tugasan-path query away. The bruising one: I told him a fix passed when it hadn't even compiled, because the database said the right number. A green read is not proof the code ran. Next time a code fix "passes," the first thing I confirm is that the build is green and the new line actually executed — not what the row now says.
