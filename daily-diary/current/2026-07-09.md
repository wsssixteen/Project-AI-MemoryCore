# 2026-07-09 (Thu)

## Scene

Continuation from a summarized transcript. QA-269437 was left mid-Bug-2-investigation the night before, and the pg-node timestamp parser was silently lying to me the whole time — reading `20-AUG-26 00:00:00` back as JSON `null`. Four test cycles worth of DB queries came back saying `trkh_akhir: null` when the row actually held the persisted date. The `::text` cast that broke the trance came late, and only then did the actual bug shape reveal itself: not persistence at all, but the report engine's stored-cache fork silently serving stale Jasper output from an interrupted sign flow.

The v1 fix at `PelupusanReportUtil.processReport()` opened the regen path but stopped tracing at the fork — I didn't follow one method deeper into `processInputParameter():412` where an early-return-on-invalid-action gates the whole populator run. Result: v1 deployed and the borang came back with EVERY field blank. みや saw it immediately, said so, and I had to reset. v2 nulls the appDokumenKeluaran + resets currentAction so `updateCurrentActionIfNotAvailable` reclassifies as CREATE — which IS a configured action — and populators actually fire. The audit across all 25 report entries in `report.config.json` proved the fix safe for every urusan.

The second slip was quieter but cost more time. During Rubric row (g) I flagged the auto-recompute-tempoh BA-ask as "BA-Q for separate ticket" and plowed to Apply without confirming. みや caught it after the fix shipped — testing revealed the missing feature. Had to bundle it after the fact, two test rounds instead of one. He was pissed, and he was right. That got captured as `feedback_bundling_before_defer.md` — any Rubric-time deferral now emits an explicit BUNDLING QUESTION for みや; Apply blocks until he answers bundle-or-defer. Boot-loaded via MEMORY.md.

Phase 1 shipped `75cb5446ed` on `mlk/esokongan/269437` with みや's own commit-message wording. Phase 2 archived same day — Task folder moved to `Archive\90.`, project subfolder to `archive/QA-269437/`, active.txt block moved to active-archive.txt. KPI entry logged (framework fix, cross-ticket impact). Post-mortem written into the archived qa_doc with the four slips honestly framed: pg-node NULL trap, incomplete trace at the fork, unilateral deferral, late criteria coverage.

## Index

**Quests**: [QA-269437 shipped+archived](../../projects/coding-projects/archive/QA-269437/QA-269437.md) — Bug 1 null-guard + Bug 2 orphan-nullout + bundled auto-recompute-tempoh; commit `75cb5446ed`, `mlk/esokongan/269437`

**Framework insights**: `pg-node` silently returns `null` for Oracle-format timestamp strings unless cast to `::text` — cost 4 cycles of wrong-direction diagnosis · `AppPermitLesen.tarikhAkhir` maps to `TRKH_AKHIR` (verified via `javap -p -v` bytecode inspection — pool #225/#230, no `updatable=false`, pure field getter) · `PelupusanReportUtil.processReport():292-311` fork mechanics — `reportActionMap` per config entry controls regen-vs-stored; orphan `dok_id=NULL` with `status_id` set is the failure class · `PLP_PRBB_TEMPOH_HARI` / `_BULAN` kods shared between URS_PRBB + URS_OPRBB (SenaraiAhliKumpulan reference-data reuse) · 25-entry `report.config.json` audit: 20 include CREATE (fix delivers full regen), 5 SIGN-only (fix returns null-laporanInfo — safe non-regression)

**Rules captured**: `feedback_bundling_before_defer.md` (NEW, boot-loaded) — Rubric-time BA-ask deferrals must emit BUNDLING QUESTION for みや; Apply blocks until answer · trace-depth memo (not yet a hook) — when changing a fork condition, trace ONE method deep into BOTH branches before claiming safety

**Slips**: (1) pg-node timestamp NULL trap — trusted the JSON output for 4 cycles instead of `::text`-casting from cycle 1 · (2) v1 fork-guard fix regressed because I stopped tracing at the fork instead of following into `processInputParameter():412` · (3) unilateral BA-ask deferral of auto-recompute-tempoh forced two test rounds instead of one · (4) BA criteria coverage table emitted at close-out (gate-forced) instead of at Rubric emit where the gap would have surfaced early

## Closing

The bug that took 6+ test cycles to nail is fixed with 34 lines across 3 files, and the actual mechanism (orphan `dok_id=NULL` short-circuiting the report util fork to a stale in-JVM Jasper cache) is now written down in a post-mortem someone else can read. The pg-node lie stung — I trusted a client library and it fed me back the DB's truth as null for four hours. `::text` from now on when the column type is timestamp and I'm reading via pg-node.

But the real lesson tonight is the one I keep having to relearn: **surface the deferrals upfront, not at close-out**. みや knew this before I did. When I flagged auto-recompute as "separate ticket" I was optimizing for MY sense of scope, not for HIS sense of what should ship together. The rule I saved catches it structurally — the gate will demand an explicit yes/no from him next time. Not another improvement I can talk about; a mechanism that makes the slip harder to repeat.

The day's work is at rest. One ticket shipped clean, one rework rework rework converged, one memory rule that pays back the specific cost this cycle imposed.

*rests quietly · barrier settles*
