# 2026-06-14

## Sessions

### Session 1 — Phantom QA-261986 cycle-2 re-quest (Opus 4.8 → fast Opus 4.7)

Late-night session (02:00–02:30 MPST). みや typed `/quest start 261986` with strong "verify each step / read annotations / confirm CC tags populated codewise / verify twice / end with test scenario / template at E:\...\MLK named refer photo". Started a fresh worktree (`admiring-ardinghelli-7b0b46`); session-start active.txt block read as `status=active phase=0 current_phase=Recon` — appeared open.

Drove the full quest flow against that read:
- Loaded Task folder + cycle-1 history (the 492-line QA-261986.md, BA's cycle-2 PDF, the "ahli lembaga yang salah" PNG).
- Extracted PDF annotations via `pypdf` after fitz segfaulted on Python 3.14 (PyMuPDF native crash) → installed pypdf inline → captured 18 annotations with exact RGB colors + comment bodies (red `(0.92,0.29,0.29)` = fix, green `(0.49,0.94,0.4)` = OK). BA's red comments included verbatim *"Lapan Ratus Lima Puluh Lima Ringgit"* and *"PENTADBIR TANAH <DAERAH>"* plus a full §5.4 expected sentence.
- Dumped SDT tags from all 4 PSBS Risalat templates (Syarikat Lulus/Tolak + Individu Lulus/Tolak) → confirmed the Syarikat templates were missing every cycle-1 tag (`ahliLembaga`, `nilaianPasaranJPPH`, `nilaianPasaranJPPHPerkataan`, `premiumPTPerkataan`, `kadarNilaianJPPH`, `tempohPajakanDipohon`, `daerahPejabat`, `syorKeputusan`, `dunYB`, `tarikhSuratJT`, `jabatanTeknikalPSBS`).
- Verified populator wiring: `WORD_CONTENT_CONTROL_METHOD` map at `PelupusanWordCCMethodConstant.java:181`, no `jenisPemohon` guard — every populator the Syarikat template would need is already globally registered (cycle-1 shipped that).
- Spawned a read-only Scout familiar over the 18k-line constant file. Scout returned a thorough table but claimed the §2.1.2 board-of-directors bug was a "code fallback fetching the company" at lines 3190-3194. Recon caught that as **dead code** (the method returns at :3182 before that branch). Real cause: the Syarikat template's §2.1.2 PARA 13 uses tags `namaPemohonLower` + `jenisNoKP` + `noPengenalan` (the applicant) instead of the `ahliLembaga` table SDT. Confirmed by CC-tag dump (no `ahliLembaga` tag in Syarikat) + working analogs (`TemplateRisalatMMKNSyarikat_PDT_PPJK/PRU.docx` use `ahliLembaga`) + DB (director MIRA `010203-04-0506` exists in `umm_a_ahli_lembaga` for aplikasi 2963425).
- Built a surgical python/zipfile script with 18 exact-match edits + header propagation. DRY_RUN passed (every old-string matched count=1). Wrote the file. Hit a transient Windows write lock; PowerShell `Move-Item` with retry got it in. Final consolidated verification: testzip OK, well-formed, no duplicate `w:id`, 19/19 headers carry `idPermohonan`, all 12 new tags present, 5 old tags removed (functional swaps complete; `bandarPekanMukimDipohon` 2× remaining are the legitimate §1/§2 mukim refs).
- Caught a missed item at verify-time: §3 `jabatanTeknikalPTSyarikat` → `jabatanTeknikalPSBS` swap. Applied as edit 19 (single tag swap with assertion + integrity check).
- Recorded cycle-2 detail to QA-261986.md (cycle-2 section + edit-19 addendum) and updated active.txt via `active-cli.js` (current_phase=Apply, cycle2_lulus="applied-uncommitted...").
- Built the test scenario with full Test data row, "WILL populate" expectations (MIRA in §2.1.2, valuation 6000 + "Enam Ribu Ringgit", premium words "Lapan Ratus Lima Puluh Lima Ringgit", kadar 5600), DATA GAPS (no JT dept ulasan rows, no YB row), and the §2.1.3 "0 Tahun" BA-Q.
- When asked "list all fixes done + double-check code changes" — checked git in etanah-pelupusan and found commit `a8bc2c4f2f` (yesterday by me): `QA #261986 - PSBS - Risalat MMKN Syarikat fix` — 6 files including BOTH Syarikat Lulus + Tolak + 42 lines of Java. **The cycle-2 was already shipped yesterday.** Tonight's session was a phantom re-quest: session-start active.txt was a stale snapshot from when the worktree forked, and didn't see yesterday's DE commit `8eb10a7` that flipped QA-261986 to `status=closed`. The edits I wrote tonight via python produced byte-identical output to the already-shipped file → git showed no diff → no harm done, but the entire investigation was wasted re-work.
- Confirmed §2.1.3 "0 Tahun" tempoh pajakan asal is the **stored DB value rendered faithfully** (`umm_a_hkmlk.tempoh_pajakan=0` for aplikasi_id 2963425) — not a template/populator bug, only a BA-Q about business correctness. After みや asked "perform domain expansion and commit and merge to head", switched into DE: pulled `origin/main` into the worktree (was behind by 1, the yesterday DE commit), confirmed working tree clean, wrote this diary + slip-log entry, committed + pushed.

The work product of the session: zero new fixes, one solid slip lesson (the phantom-quest mechanism), refreshed annotation tooling (pypdf install + a CC-tag dump script + an exact-match XML diff script that all live in `.claude-tmp/`).

## Index

- **Slip lessons**: Session 1 — phantom-quest from stale worktree active.txt (new slip class)
- **Tools / infra**: `pypdf` now installed in user Python 3.14 (fitz segfaults on this version); reusable scripts in `.claude-tmp/`: `qa261986_pypdf.py` (PDF annot extraction), `dump_sdt.py` (CC-tag dump across templates), `fix_lulus.py` (DRY_RUN-then-write surgical .docx editor with exact-match gates)
- **Knowledge surfaced**: Scout familiar reports need adversarial Recon every time — tonight's Scout cited the §2.1.2 bug at lines 3190-3194 (dead code); the real cause was elsewhere. The pattern (read-only Scout draft → Recon distrust + verify) caught the error.

## Closing

I learned the cost of a stale boot snapshot tonight. The session was technically clean — diagnosis was sharp, the Scout-caught-Recon flow worked, the test scenario was honest about data gaps — but the entire 90 minutes was spent re-deriving a fix that had been pushed to master at 17:00 the previous day. The worktree's active.txt didn't know yet because no one had merged origin/main into it.

The honest read: my session-boot check stops at "read active.txt" — it doesn't reconcile against `git rev-list HEAD..origin/main` for state files that DE pushes nightly. For an open quest with an in-flight fix, that gap means a phantom re-quest is one stale read away. Worth a hook that surfaces "your worktree is N behind origin/main on these tracked state files" at boot, not just at DE close. Filed in the slip-log as `phantom-quest-from-stale-active.txt-across-worktree-boundary`.
