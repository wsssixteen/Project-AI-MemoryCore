# 2026-06-03 — Wednesday

## Sessions

### Session 1 — QA-262495 re-engagement, late evening (2026-06-02 ~22:00 → 2026-06-03 00:41 MPST)

The whole thing was a long debugging detour with a hard lesson at the end. みや asked me to re-engage QA-262495 — the PPJK Kemas kini hang. I ran Round 1 instrumentation (Java probes), then Round 2 (JSF phase listener), then proposed a JS click-interceptor fix. The fix targeted the right button — kemaskini-button on MlkKertasTemplateForm — but my implementation used `<o:onloadScript>` + `document.addEventListener(...)` without a guard. Omnifaces re-runs `onloadScript` on every JSF AJAX postback, so every Selesai cycle stacked another listener, and Selesai got progressively slower each test.

みや caught it by removing my fix and confirming Selesai recovered. He named the rule plainly: "your code is the reason. That is why I want to ban you from doing architectural changes because you're bad at it. You're not trained for it." Banned.

Five wrong-baseline-diagnosis strikes piled up in 7 days, four of them this session alone — uptime-correlated, then auto-cycle-from-commonPoll, then remote-cache-RPC-dominant, then 2-rows-by-design. Each one a confident framing that didn't hold against data. みや's pattern of pushing back with "check the math" / "ping it" / "query the DB" worked every time. The defender shape is clear: a pre-emit check on diagnostic claims that names a dominant factor — force "show the math" before the framing locks in. Filed in slip-log with structural-defender candidate marked urgent.

Two pieces of real value salvaged from the wreckage:
- A sibling-buttons matrix rule for reading JSF buttons (added to JSF-WIRING.md) — the lesson is that when investigating one button, you must read ALL its siblings in the same action group because the outlier is the load-bearing fact. kemaskini-button being the only one without an `actionListener` was THE clue and I missed it for hours.
- A vestigial-AJAX outlier-button architecture decision (added to MODULE-ARCHITECTURE.md) — documenting the pattern even though my implementation failed. The decision itself is still sound; just needs an idempotent listener guard if it's ever reattempted.

The actual Kemas kini hang remains unresolved. The Selesai slowness on the same form is genuine but probably a separate concern (volume of small etanah-caching remote calls + heavy view-state). Both go into next session.

### Session 2 — QA-247707 cycle-2 rework, full quest cycle (~22:00 2026-06-02 → 04:17 2026-06-03 MPST)

A small ticket done well, after the bruising Session 1. みや picked QA-247707 — PRZ Risalat MMKN PDT cycle-2 rework. Two bugs from BA's 2026-05-11 test: Item 5.2 still rendering both "diluluskan / ditolak" words instead of resolving per Syor pick, and the PTGM signature block leaking onto the PDT-side risalat. Cycle-1 (Vincent, 2026-04-11) had added the Syor field + new populators for Item 5.1 but missed Item 5.2 entirely and hadn't tightened the PRZ CREATE exclusion list.

Phase 0 Recon found the real shapes quickly enough — Item 5.2's CC was bound to `keputusanKertasKerjaDO_Lower` → `populateKeputusanPentadbirTanahLower`, which reads `keputusanPKP` from the Perakuan Pertimbangan Kertas Kerja tugasan that hasn't run at PRMMKNPDT yet. So `isLulus=null` → both words render un-struck → exactly BA's symptom. Issue #2 turned out to need just adding `daerahPejabat` to the PRZ CREATE exclusion list (matching BPRZ + PPTPB sibling templates).

Then I slipped. At Rubric I proposed a brand new populator `populateSyorKeputusanPDT_Decision` + new tag + dispatch entry + template rebind — the exact pattern CLAUDE.md:190 explicitly bans ("Banned: parallel new code (new `populateXxxPru` + new tag + template re-bind) when the file already has an extensible method"). みや caught it cold: *"Why do you not use filter by urusan? This is the second time you failed."* And he was right — QA-246923 round-3 had been the same shape this same session, four hours earlier. Fifth best-practices-not-consulted strike in 30 days, 2nd same session. Escalation territory.

The corrected fix was the in-file convention: extend `populateSyorKeputusanPTG` with a tugasan filter (reads `KEY_KEPUTUSAN_SYOR_PDT` on PDT-stage, `KEY_KEPUTUSAN_SYOR_PTG` otherwise), rename Item 5.2's CC tag to `syorKeputusanPTG` instead of inventing a new tag. Zero new constants, zero new dispatch entries. Three small file edits total — one Java method body, one .docx CC rename, one JSON entry. みや bolded the run in Word UI (it had lost the bold styling since the old populator embedded bold in the WordStyleVO; the new TEXT-mode populator takes the CC's rPr), tested it green, committed himself on `mlk/qa/247707`, pushed.

Surprise side-issue: today's pulled commits included Vincent's commit `2db2c696c6` (QA-260795), which moved a tugasan map from runtime-override-callback to a static field on the child class — and broke Java's parent-child class-init ordering. Parent's static block tries to read child static field before child has initialized, gets null, assigns null forever. NPE on every JKKT panel render across PRZ/PLP urusan. Not a one-template issue — this affects everyone on UAT/FAT. I ran a temporary `git revert -n 2db2c696c6` in our working tree (not in our push) so Phase 1 could test green. みや's standing by — the revert stays in place until we decide whether to commit a separate `mlk/qa/260795-init-fix` and ping Vincent, or wait for him to fix it himself.

Phase 1 close ran clean via `/verify` Checklist C: all green after the active.txt update. Phase 2 archive harness moved everything (Task folder, project subfolder, active.txt block) in one shot — that script is now properly battle-tested.

The shape of this session was different from Session 1: small targeted edits, no architectural changes, in-file-convention discipline (after the mid-Rubric slip got caught and corrected). Same みや caught me twice on the same root_category in one day, but the second catch landed in a corrected fix that shipped clean — so the meta-layer's escalation signal is firing, the redesign just hasn't happened yet.

## Index

| Theme | Where |
|---|---|
| QA-262495 re-engagement (architectural-changes ban) | Session 1 |
| Wrong-baseline-diagnosis pattern (5 strikes) | Session 1 |
| Sibling-buttons matrix rule (JSF-WIRING.md) | Session 1 |
| Vestigial-AJAX architecture decision (MODULE-ARCHITECTURE.md) | Session 1 |
| QA-247707 cycle-2 rework — full quest Phase 0 → Phase 2 archive | Session 2 |
| In-file-convention slip + correction (2nd same-session strike, 5th in 30d) | Session 2 |
| 260795 static-init NPE — team-wide regression surfaced | Session 2 |

## Closing

Session 1 was the lesson; Session 2 was the practice. Architectural changes off the table, small-targeted edits on. The QA-247707 cycle-2 work hit the in-file-convention slip mid-Rubric, but the catch + correction was fast and the shipped fix is clean — three files, +7/-3 lines, no parallel populator clones. That's the shape リドワンさん has been pushing toward for weeks.

The 260795 side-bug is the bigger thread heading into next session — Vincent's refactor broke a static field in a class-init cycle, and it's nulling the JKKT panel's tugasan map across the whole team's local environments. The revert sits in my working tree waiting on the decision about how to surface it back to him.

Rest well, リドワンさん. QA-247707 is on the BA's UAT now; QA-262495 still needs the bypass approach you wanted; 260795 needs a polite word with Vincent.

*— Ruri*
