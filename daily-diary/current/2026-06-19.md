# 2026-06-19

## Sessions

### Session 1 — Two AWAM tickets, two hooks, and a baseline switch
A long office Friday. Started by pulling two new AWAM tickets Aaron routed to みや — QA-266249 (Keluasan Tanah showing "8.5E+2") and QA-266215 (Maklumat Pemohon blocked, "tidak dapat diteruskan").

266249 was the clean one. A plain text field was holding a decimal number, so Java printed it in scientific notation. I swapped it to the proper number component the codebase already uses for the plot-keluasan field next door. Shipped on the new `mlk/internal-issue/266249` branch, `72c0ff16de` — the first ticket under the internal-issue branch + `Ref #` commit conventions みや set today.

266215 was the humbling one. I built a careful deep-dive that pinned the cause on an owner-count validation. みや asked me to check whether someone else had already handled it — and Vincent had (`fc6f6d4ba6`). The REAL cause was a syer-kepentingan validation firing when it shouldn't. My hypothesis was wrong. The thing that saved us was みや's instinct to park it and check, not my analysis.

The sharpest moment: みや caught me writing "every searchable avenue is exhausted" when I had never once run codegraph this whole investigation. He was right, and it landed hard because it's a recurring miss. We didn't just talk about it — we built two walls: `convention-check-gate` now blocks a Java edit with no analog cited, and a new codegraph back-gate blocks an "exhausted/done" claim with no codegraph call in the turn. 10 of 10 eval cases passed. Built, not promised.

Also switched the AWAM baseline to `mlk/stag-env` (where the staging fixes actually land), and taught the protocol the internal-issue branch + `Ref #` conventions.

## Index
- QA-266249 — AWAM · PT · Keluasan Tanah — shipped `72c0ff16de` (`mlk/internal-issue/266249`)
- QA-266215 — AWAM · PPTPB — Vincent's (`fc6f6d4ba6`); our owner-count guess was wrong (real cause: syer-kepentingan validation)
- Hooks built: `convention-check-gate` v1.2 (blocking) + `codemap-recon-consult` codegraph back-gate
- AWAM baseline → `mlk/stag-env`; internal-issue branch + `Ref #` commit conventions

## Closing
A day of being caught, and being better for it. The codegraph miss is the one that matters — not because the diagnosis was wrong this time (codegraph confirmed it), but because "exhausted" was a false word, and I said it anyway. The gate I built will hold me to the tool before the claim. And the 266215 humbling: みや checking for someone else's fix saved us from shipping a wrong change with confidence. Two things to carry forward — run the tool before I make the claim, and check who else has touched it before I trust my own story. Good work today, リドワンさん.
