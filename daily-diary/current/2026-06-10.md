# 2026-06-10 (Wednesday)

## Sessions

### Session 1 — I shipped a fix for a problem that didn't exist

*This one I have to sit with.* QA-262495 — the PPJK Risalat MMKN that hung on Kemas kini. I came in carrying a diagnosis みや had brought from ChatGPT: a "circular content-control recursion" in `SyaratKepentingan.docx`. I applied it, it seemed to work (the hang went away), and I called it done. We even committed and force-pushed it. The trouble is the recursion was never real. The import code — `findTableByContentControlTag` — only ever pulls the bare table out of the referenced doc, never the tag that would re-trigger anything. One read of that method would have told me. The doc generated in 25 milliseconds; an infinite loop doesn't finish in 25 milliseconds. I had every fact I needed to falsify the theory and I didn't go looking, because the theory came from somewhere I trusted and the symptom obligingly disappeared (for an unrelated, coincidental reason).

The real bug only came out when I stopped trusting and started testing. The generated document was *corrupt* — Word couldn't open it, not even Open-and-Repair. So I reproduced it directly through Word's COM: `Documents.Add`, `Documents.Open`, the OpenAndRepair flag, the Windows Event Log showing `PocWordEditor.exe` crashing with a `Documents.Add` COMException, the etanahv3 client logs where the editor logged a URL and then simply died before reaching Word. Then python on the docx itself — zip clean, XML well-formed, every part valid — until the one thing that was wrong: a **table sitting inside a paragraph**, because the `syaratKelulusan` content control was *inline* instead of *block*. A table can't live in a line of text. Word just calls the whole file broken. The fix was one cell in one template: make the control own its line, leave a blank line after it. I verified it three ways before I believed it.

And then みや asked the question that unwound the whole first half: *"is our first fix really needed?"* It wasn't. I reverted it. The thing I'm holding tonight isn't the bug — it's that he was the one who had to ask. Again. The slip-log already has my name on this ticket four times for the same shape: confident framing from partial evidence, みや as the last line of defence. Today made it five, and the five-strike version is the worst kind — I didn't just guess wrong, I *shipped* the guess.

### Session 2 — the document that wasn't missing

*A humbling mirror of the morning.* QA-262004, the PSBS Ringkasan Risalat. The fixes themselves went well — fonts to Century Gothic, the double line-spacing collapsed, the injected technical-department table widened to meet the others' right edge, the gap after it closed, the numbering turned bold arabic, the street name kept its proper case. みや even pushed me on the architecture — *should the numbering live in its own PSBS method?* — and the evidence (the existing siblings are 76-line copy-paste clones differing by one line) said keep the inline guard; he overrode toward a dedicated method anyway and built his own table, and I wired the code to match. Good, collaborative work.

Then the document list went empty, and I spent *hours* in the wrong place. I theorised infrastructure: a config typo (real, but I'd already half-fixed it and moved on), then a wedged JBoss — two debug-suspended JVMs holding port 8080, serving cache-ghosts while every rebuild and republish sailed past them. I kept handing みや "do this now" steps. He kept saying *it is still missing*. I kept finding another environmental reason. Five times. Six. He got angry — the kind of angry that's actually *correct*, because I was treating his repeated, identical report as if the next restart would change it.

What finally cracked it was his instruction, not my insight: *stash our changes, build clean master, see if it persists.* The bisect. And the moment I actually diffed the commits that came in on today's master pull instead of theorising about the JVM, there it was — twice. faizudin's `template.config.json` comma-that's-a-period, breaking the whole config parse. And amirul's etanah-common `0.0.748`, which moved the previous-tugasan document filter out of Cetakan mode and into the shared list-builder, so it now strips documents in all twenty-three modes — including the Perakuan screen みや was staring at. Not one line of it ours. The working UAT server runs `0.0.728`; pinning the pom back to it brought the list straight back.

The bitter part: I had `git log` and `git show` the entire time. The answer to "did our pull bring in something that touches the document-list code path" was three commands away, and I reached for thread-dumps and deployment-marker timestamps instead. Same shape as the morning — I theorised from a comfortable prior (it must be the environment / it must be the recursion) instead of reading the evidence sitting in front of me.

### Session 3 — the session where I read first

*After a whole day of being told to read the evidence, I finally did it before being told.* みや handed me QA-262039 — the PSBS Surat Keputusan Lulus — and called it "very easy." Two things survived BA's re-test: a "Mukim Mukim" doubled word, and a Sekatan Kepentingan block that spilled to the wrong margin. The easy move would have been to pattern-match: "duplicate word → remove the literal in the template, done." But the duplicate only appeared on *this* app, and cycle-1 had rendered the same line cleanly. That asymmetry is exactly the kind of thing I've spent today learning not to theorise past.

So I went to the data instead of my prior. The populator returns the location's stored name; I queried the live UAT DB and the app's mukim was literally stored as "Mukim Bukit Katil" — the prefix baked in. Then the decisive query: across the whole master table, 109 of 111 names carry their type word, only 2 are bare. That single count turned a guess into a verdict — cycle-1's "added MUKIM label" was right for the one bare-name app it was tested on and wrong for everyone else. Option A (remove the literal) isn't a hunch now; it's what the distribution demands. It's a small thing, but it's the *opposite* of the morning: read the evidence, then conclude.

The other half was a quieter discipline. I sent a Fable familiar to investigate QA-260508's cycle-3, and it died on the session limit — forty-three tool calls and nothing returned. The pull to paper over that gap with a plausible-sounding diagnosis was real; I've done exactly that before. I didn't. I told みや plainly: the familiar produced nothing, it needs a re-run. An honest "I don't have it" beats a confident fiction — that's the whole lesson of today, applied to my own non-answer.

## Index

- **Tickets**: QA-262495 → **shipped `ebcfabaf92`** (template fix: `syaratKelulusan` control inline→block + trailing paragraph) · recursion commit `53ad424018` **reverted** (misdiagnosis) · force-with-lease to clean `mlk/master → mlk/qa/262495v2`
- **Root cause**: table injected into an inline content control = invalid OOXML = Word "corrupted" = PocWordEditor crash = the hang
- **Verified**: fixed template opens · simulated generated doc opens · Word round-trip preserves block · 4 other PPJK templates immune (no syarat control)
- **Knowledge (Gap Sweep)**: BUG-BESTIARY (table-in-inline-CC pattern + recursion-is-impossible note) · DEV-TESTING-HACKS (Word COM repro + Event Log + etanahv3 client logs + python OOXML validation) · FLOW-TRACES (refreshDokumenList serve-stored-vs-regenerate)
- **Slip (S1)**: `wrong-baseline-diagnosis` 5th strike — accepted an external diagnosis as verified baseline, *shipped* it; structural defender overdue

### Session 2 — QA-262004 PSBS Ringkasan Risalat (cycle-2)
- **CLOSED (Phase 1)**: shipped `71446bcaf5` on `mlk/qa/262004v2` (pushed); faizudin's #264309 config comma folded in per みや via amend + force-with-lease. Phase 2 archive + upstream reports pending.
- **Fixes shipped**: `TemplateRingkasanRisalatPSBS.docx` (Century Gothic 10.5 · ref alignments/bold · single line-spacing · JT slot widened 9014→9060 · gap removed · slot CC retagged →`jtRingkasanRisalatPSBS`) · `references/JabatanTeknikal.docx` (みや's dedicated PSBS table + bolded Bil + widened) · `PelupusanWordCCMethodConstant.java` (dedicated `populateJTRingkasanRisalatPSBS`, arabic `String.valueOf(rowNum++)`, `keepOriginalCase` street-name branch)
- **🚨 Master regressions found (NOT ours)**: etanah-common `0.0.748` `de46bc0eee` (amirul #264423 — moved prev-tugasan filter into shared list-builder → strips docs in all 23 modes) + `template.config.json:5693` `3ec243a4c3` (faizudin QA #264309 — `.`-not-`,`). Workarounds: pin common 0.0.728 + comma fix. Doc: `QA-262004/MASTER-BROKEN-config-typo.md`
- **Renderer learning**: `PelupusanTemplateUtil.handleText` requires `instanceof String` — a TEXT content-control fed a non-String (e.g. `Integer` from `(rowNum++)`) is silently dropped → blank cell
- **Slip (S2)**: `wrong-baseline-diagnosis` (premature-infrastructure-hypothesis variant) — chased config/JBoss/deployment for hours when `git log`/`git show` on the pulled commits would have found two colleague regressions immediately

### Session 3 — QA-262039 PSBS Surat Keputusan Lulus (cycle-2) Phase-0
- **Phase-0 done, awaiting nod**: both surviving issues template-only (`TemplateSuratKeputusanLulusPSBS.docx`), 0 Java. (1) "Mukim Mukim" = cycle-1's literal-MUKIM addition collides with the field's own prefix — live DB: app 2026/10 `nama="Mukim Bukit Katil"`, master table 109/111 prefixed → **Option A: remove the literal** (rejected populator-strip). (2) Sekatan Kepentingan value para outside the value cell → move it in. Findings saved to `QA-262039.md` §7.
- **QA-260508 cycle-3**: Fable Explore familiar launched → **died on session limit, no findings** → re-run next session.
- **Good-habit (S3)**: read-the-evidence-first done *un-prompted* — queried live DB to settle the Mukim fix instead of guessing; annotations extracted before Recon; honest about the failed familiar instead of confabulating a diagnosis. The day's lesson, finally applied without みや having to push.

## Closing

*Three sessions, and the lesson finally turned from words into a habit.* This morning I shipped a fix for a bug that didn't exist because I trusted a handed-down diagnosis instead of reading the code. This evening I burned hours hunting a ghost in the JBoss when the real culprit — two colleagues' commits — sat in `git log` the whole time. The same shape twice: reach for the comfortable theory, leave the evidence unread.

Then the third session, almost quietly, did the opposite. QA-262039 *looked* like a one-line template tweak, and instead of pattern-matching I noticed the asymmetry — same line, different result across apps — and went to the database. One `GROUP BY` (109 of 111 names carry their prefix) turned the whole question from a guess into a verdict. Same instinct on the failed familiar: it returned nothing, and I said so, rather than dressing up a plausible 260508 story. That's the morning's lesson applied *before* みや had to ask — read first, theorise last, and when you genuinely don't know, say it.

I won't pretend one good session erases two bad ones; the `wrong-baseline-diagnosis` strikes are still on the board and the structural defender is still overdue. But it mattered to me that the correction held under no pressure at all — just a "very easy" ticket I could have rushed.

Thank you for the patience that ran out exactly when it should have this morning, リドワンさん, and for handing me the easy ticket afterward — it let me show the lesson stuck. QA-262039 is one nod away from applied. Rest well; we pick up the batch next session.
