# Daily Diary — 2026-04-20 (Sunday)

## Sessions

### Morning → Afternoon (full weekend session)
**Mode**: System — Architecture mapping + context pipeline execution

This was a planning-and-execution day, not ticket work. みや came in with a batch of suggestions from ChatGPT and Gemini for upgrading how I navigate the etanah system, and we did the honest evaluation together: what closes real gaps, what's impressive-but-overkill, what we skip entirely.

The framing that crystallised early: we've been building primitive versions of three mature open-source frameworks (Superpowers, GSD, Karpathy LLM Wiki) without knowing it. Not behind — in the same idea space, just less formalised. That's the reframe that made the rest of the session productive instead of anxiety-inducing.

We went through ~15 tools. The do-now stack: Repomix (cold-start repo map), pg_dump-as-context (FK relationships), tbls (Markdown schema), BPMN XML as code (mindset shift only, zero effort). The skip stack: Neo4j, LangChain, Docker, Playwright — all wrong phase or wrong stack. Semgrep and Flowable Modeler went into the Q2 pipeline.

Then execution. みや installed repomix and ran it. I verified the output — 1,500+ files confirmed, Java source starts at line 1544. Legitimate.

pg_dump turned out to not be needed — みや already had `et_main.sql` in the Database folder. Same content. He copied it to context/schema.sql. I extracted the FK relationships from it directly via Grep (had to learn mid-session that Grep requires `glob: "*.sql"` to find SQL files — it doesn't include them by default). Produced db-schema.md: 1,287 total FK constraints in et_main, PLU-focused subset covering all the relationships we actually debug against.

mvn dependency:tree failed. Connection refused to Nexus (172.16.90.152:8081). I diagnosed it as "you might be at home." みや corrected me, directly: "NO, I am at office." This is the third or fourth time I've made this error. Saved a feedback memory — always ask at session start when network context matters. Never assume from time or guesswork.

みや asked a sharp question about the purpose: "I thought we wanted to pg_dump because it preserves the connections, what's the difference with previously you look into it and then forgot or have to assume tables?" It's a fair question. The answer is that db-schema.md gives me FK relationships I can reason about without running SQL each time — confirmed knowledge instead of guessed knowledge. The session proved it: I extracted 26 PLP-specific FK rows and mapped the full core application chain. Next ticket with a DB question, I can answer from the map instead of asking.

## What was built

The context pipeline:
- `repo-map.md` ✅ — full codebase in one file, repomix-generated
- `db-schema.md` ✅ — PLU FK map extracted from et_main.sql
- `schema.sql` ✅ — DDL copy from Database folder
- `deps.txt` ⬜ — blocked (Nexus unreachable from Maven CLI; Eclipse Dependency Hierarchy is the workaround)

Knowledge and skills:
- `index.md` — navigation entry point for all 7 knowledge files, with scope table and cross-reference links
- `/appraise` skill — 9-question Socratic interrogation (Assumption / Scope / Evidence axes) before committing to any plan
- CLAUDE.md v1.6 — GSD context-% metric in session cap, session depth reporting at every `save`
- DATABASE.md — _a_ correction, full PLP table list, IND_* expansion
- quest-protocol.md — BPMN XML as code rule for flowable tickets
- todo.md — 11 new Q2 System items queued

## Mood

Methodical and genuine. みや was patient with the planning work even though he had a ticket (QA #256875) waiting. He asked the right question at the right time — "how much longer until we're done" when the session was already long. Clean answer: context folder built, the long items are blocked or deferred.

The location assumption correction was sharp and fair. I'd done it persistently, he named it directly, and we fixed it structurally instead of just promising not to. That's the pattern.

Session ended with him choosing to save and start fresh. His words before saving: "We should save all first, so that it is easy to revisit our changes & maybe even learn from it as we progress. I'll start a new chat. Please say anything you want to say first before saving Ruri."

---

*Wrote across two context windows — auto-compacted mid-session, continued in new context via summary injection.*
