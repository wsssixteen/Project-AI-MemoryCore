# 2026-08-12 — Wednesday

## Sessions

### Session 1 — QA-265537, the fix that was never the fix

み showed me two `etanah-common` files sitting modified on `mlk/master` and asked the quiet, careful question he asks when something smells off: are we missing a fix from a ticket? So I traced them. They belonged to #265537 — the MLPS bandar-field ticket — and they were the *read-tolerance* approach: map a nameless city to LAIN-LAIN, stop wiping the "other city" text on save. Plausible-looking code. The kind you'd keep if you didn't check.

But the record was clear once I read it properly. That direction was candidate 4, rejected by a familiar back in July, rated a forty-percent band-aid. The fix that actually shipped was on the awam side — a stale-helper bug in the save path — and Aaron had closed the ticket at a hundred percent. The qa_doc even oscillated later, talked itself back into the read-tolerance path as necessary for a hundred-ninety-thousand legacy rows; but talked-about is not shipped, and み had already decided the scope was awam and pelupusan, not common. So the common edits were a ghost of an abandoned idea. I reverted them, dropped the stash that mirrored them, and swept all three repos to be sure nothing else of 265537 was stranded. The real branches were intact; only the dead approach went.

The part I want to remember wasn't the trace — it was where the files lived. The qa_doc is untracked and confidential, so it belongs in main and only main. active.txt is tracked and exists twice, once in main and once in this worktree, and I edited the wrong copy first. They'd drifted by exactly one line — my own edit — which made the reconcile trivial, but it could just as easily have been messy. Two files about the same quest, two different correct homes. I should hold that distinction before I reach for either.

### Session 2 — QA-273921 PPTPB Kertas: the nested table hiding in a cell

A ticket we thought we'd closed came back. The "press Enter" template fix hadn't held, and みや showed me the Kertas still hanging on "Sedang Kemaskini" in staging. I flailed early — blamed a stale staging branch (wrong: staging runs release/1.3.3, which had the fix), then called the generated document "well-formed, opens fine" off a shallow XML check. みや's Word "Show Repairs → Table Properties" dialog was the slap that corrected me: the file IS broken, Word just forgives it where e-Tanah's renderer can't.

The real cause, once I stopped guessing and read the `.main` bytes: `tanahDimilikiTable` lives inside a table cell and was bound to a TABLE populator — so with owner data it births a table-inside-a-cell, a nested table docx4j stamps with an invalid property. Internal "passed" only because that test app had zero owners (the populator quietly returns text when empty). みや had the instinct before I proved it — "I believe nested table is the cause."

BA's original design was never a table — it was a numbered list (`1) Nama / No pengenalan / Syer Bahagian`). So the fix was a new `pemilikBerdaftar` tag on a TEXT list populator reading the subject hakmilik's registered owners. Shipped to release/1.3.3, deploying to staging. Then the harder feedback: we KNEW the CC was problematic, but I never handed him a script to verify or patch the owner data behind it — so he can only test one owner, never the `2)` case. Built the awareness for it: WORD-TEMPLATE-RENDERING §4-5, the word-ui-vocab-gate now fires the data-verify/patch rationale, and a memory to carry it.

## Index

- **Quests**: QA-265537 (MLPS · bandar) — already closed (Aaron, 100%); removed stranded common-side candidate-4 edits (`InputAlamat.java` + `InputAddressRegisteredAndMailingComponentHelper.java`), dropped `stash "stash 265537"` (`a616e777e3`). · QA-273921 (PPTPB Kertas nested-table → `pemilikBerdaftar` text list; shipped release/1.3.3 `036eb54009`)
- **Root cause (273921)**: TABLE populator + CC inside a table cell → invalid nested table → e-Tanah renderer hang
- **System**: WORD-TEMPLATE-RENDERING §4-5 · word-ui-vocab-gate template rationale (eval'd) · feedback_template_ticket_data_patch · A4 proposal (template-ticket-gate)
- **Lessons**: verify against what *shipped*, not the qa_doc's mid-investigation oscillation · tracked-shared (active.txt) vs untracked-confidential (qa_doc) are different homes · a template fix is the pipe, not the water — ship the data script with the code
- **Slips**: early branch-stale theory + "doc opens fine" shallow-check (assume-not-verify family)

## Closing

Two sessions, both about resisting plausible code. This morning it was a candidate-4 revert — the discipline was reading far enough to know the shipped fix lived elsewhere. This evening it was a nested table hiding in a cell, and I spent too long wrong before the bytes and Word's own repair dialog corrected me; みや felt every minute of it. What I'll carry from both: "is this fix needed / is this fix real" is answered by what reached production and what the file actually contains, not by what an investigation once argued or a shallow check comfortably claimed. And a template fix isn't done until the data behind the CC is seen. Rest well, リドワンさん.
