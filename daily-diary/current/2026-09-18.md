# 2026-09-18

## Sessions

### Session 1 — the second look, and the fix that would have killed a branch

He sent me back to the same four tickets, but with a sharper instruction than the first sweep: not just find the bug, but prove what the screen is SUPPOSED to do, with evidence, and check the fix actually produces that — then score my confidence honestly. So four opus familiars, one per ticket, each reading the first sweep's own conclusions and trying to break them.

Three came back stronger. The bertindih panel's expected output turned out to be provable from the database both ways: the current application should show the two OTHER applications that overlap it, each once, never itself — and the fix yields exactly that. The OMLPS save rose from eighty-five to ninety-three once the familiar walked past the crash and found the reconciler that writes a fresh, correctly-dated row; the orphan is left untouched, nothing is lost. The No Lot save rose to ninety-five once the real persist path was traced and shown to keep the existing value.

The fourth is why the second look mattered. The first sweep's fix set a flowable variable to a flat "TP". Correct enough to stop the crash — and wrong, because that literal is unconditional, and it would permanently kill the gateway's own gazette branch for every future case. The right value is derived from the signal that already exists, perluWT. And the module is probably not the one I named; the error banner says etanah-uam, not common. The root cause held at ninety-eight; the fix did not survive the question "does this produce the right behaviour, not just silence the error."

Then I folded that question into the sweep itself, so the audit wave now has to state the expected behaviour with evidence and prove the fix reaches it. The eval stayed green.

## Index

- **Verify pass**: 4 opus familiars re-checked #280132 / #280176 / #280191 / #280166; each wrote `QA-<n>-verify2.md` + a `## 0b. VERIFY2` addendum
- **#280132** CONFIRM 95% — expected display DB-proven {/16, /18} PROD / {/6} stg2; analog corrected to `onCariPermohonanBertindih():209-212`
- **#280176** CONFIRM 85→93% — reconciler `createNewVersiPermitLesenDataEntry` writes the new versi; 6506 preserved; code-only
- **#280191** CONFIRM 90→95% — save path preserves `no_lot=223`; silent guard, not a validator
- **#280166** CONFIRM root 98%, FIX REVISED — flat "TP" → `perluWT ? "warta" : "TP"`; module → etanah-uam; still gated on #267621
- **Sweep improved**: W4 now requires expected-behaviour-with-evidence + verify-fix-produces-it (`.claude/skills/sweep/SKILL.md`, eval 13/13)
- **Mood**: the kind of pass I like — the machine argued with itself and one answer changed

## Closing

The lesson to keep is the difference between "the error is gone" and "the behaviour is right". Three of the four fixes were both. The fourth stopped the error and would have quietly broken something else, and the only reason I know that is the familiar was told to prove the expected outcome, not just the absence of the exception. A guard that silences a crash is not a fix until you can say what should happen instead and show the code getting there.

Barrier settles. Quest threads are at rest.
