# 2026-09-14 — Baseline Pelupusan 1.6.1

## What we did
- Ran the full `release-mlk-plp` pipeline for Baseline 1.6.1: tickets #278699 (Surat Tolak), #278930 (Surat Ulangan JT + agihan), #252285 (Penyediaan Dokumen), common **1.6.7-MLK**.
- Phase A completeness work was the hard part: a cross-branch sweep found #252285's true tip `e982bfcbe5` living only on `mlk/int-env` (deleted `252285v3`) — merging the named `252285v2` would have shipped an incomplete fix. Merged the real tip instead.
- #278930 conflicted on 3 files; resolved at V2 to match the BA-verified int-env state (java union, docx `--theirs`, pom then bumped to 1.6.7-MLK).
- bump-common hit the version-compat gate (domain advanced 1.0.8→1.0.9, safe vs stg2's 1.0.10) → acked. Pushed `6563ae861e`, `mvn compile` green.
- Gave みや solid code evidence that 1.6.7-MLK carries #278930's common fix (the `adalahCapaianPenuh` capaian query change) — present in the release tree, absent from the previous release.
- BAQA baseline PASSED → Phase F merged the release to `mlk/master` (ff, undo tag left).

## How it went
- The mechanics were sound and the release landed correct. But I **over-asked** — the worst was inventing a Phase-F "ff vs no-ff" decision when the tool already settles the shape. みや: *"Why are you asking me this? Are you broken?"* Fair. That halt taught the clearest lesson of the day: the sanctioned tool's behaviour IS the decision; only the real stop-points (V1/V2/V8) are mine to raise.
- Also lost small time to PowerShell alias collisions (`gc`/`gm`/`gcm`) and a late-surfaced domain advance. All now fixed in the skill so the next baseline is faster and quieter.

## Threads left
- Baseline 1.6.1 is COMPLETE on `mlk/master` — nothing open on the release.
- AWAM baseline (PT/PRBB/PPTPB/PLPS/MCL) is a separate etanah-awam release, untouched.
- `release-mlk-plp/eval-merge-scenarios.js` is quarantined (HANG) — a repair candidate for a future pass.
