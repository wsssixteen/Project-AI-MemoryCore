# 2026-09-24

## What happened
- #280895 (UPP Pembatalan, Langkah 2 blank for MCL). Re-ran the quest blind to Rubric and compared with yesterday's doc. The earlier diagnosis was built on the int-env working copy, where miya's own #279615 line already hid the real crash. On master the PRBB panel shows for MCL and reads a property the bean does not have.
- Cherry-picked that same line onto `mlk/esokongan/280895` (`0c074d07a2`), merged stag-env (`5f60caef17`). miya tested on internal, PASS. Quest closed, bounty harvested, archived.

## What I learned
- A fix that lives only on an env branch makes the bug invisible to anyone reading that working copy. Read diagnosis code from `origin/mlk/master`, and check ancestry before ruling a line in or out.
- The same permohonan ID points at different origin apps per environment (PROD UPP/03/2 = MCL, stg2 = PT). Resolve through `hubungan_aplikasi_id` on the env you test.

## Carry forward
- miya: deploy stag-env, post Redmine RC/Solution, add branch to next release.
- L9 latent: PRBB-origin cancellation still reads `keputusanMMKN`.
