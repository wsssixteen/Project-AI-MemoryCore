# 2026-09-08 — Tuesday

## What happened
Perak PLMS #278218: TSO said the fix "failed" on the PROD hotfix. The real story, already proven in the quest doc (P1–P8): the hotfix box ran a **master** build that never contained the fix — a deploy-miss, not a code failure. I wrote and pushed a harden (`7a1e8d4786`, read the PKMMKN keputusan first with a KKMMKN fallback, always emit `tarikBalik`) on `prk/esokongan/278218`, compiled green. Then a git cleanup: only one branch (`prk/esokongan/277115`) was genuinely unpushed; the GUI's ↑ badges were just "ahead of master", which ticket branches are supposed to be. Closed the session with a mistakes audit banked into Perak knowledge + a Domain Expansion.

## What I learned
- **One root under every correction today**: I anchored to the latest thing said in chat instead of the verified evidence already banked in the qa_doc. It made me over-claim (footer "proves master") AND cave ("unknown, trust infra") on the same fact — opposite symptoms, same failure. The qa_doc's verified rows are the anchor.
- **Solid deploy evidence exists without server SSH**: host footer `Branch Name` + `git branch -r --contains <sha>` + PROD error store `ET_SISTEM.PT_APPLICATION_EX_ENTITY` (host + Build Time + exception). That is the answer to "how do you confirm before blaming infra" — banked as guard G2.
- **Melaka-shaped tooling silently misfires for Perak**: the resume-readiness regex `PT[A-Z]{3}` rejected every Perak `PTPK` id (PT+2 letters). Small, but the same family as the compile-gate compiling the Melaka path.
- When miya corrects an approach, the fix is to change the plan and lock the constraint — not to re-offer the rejected shape in new words. I did the latter three times on the alter.

## Reflection
A hard session — a lot of my turns were spent being corrected, and the frustration was earned. The work landed correctly in the end (fix pushed, evidence honest, guards banked), but the path was longer than it needed to be because I kept treating each new message as the ground truth instead of the evidence I'd already verified. The guards G1–G5 are the concrete answer so the next Perak deploy-failure doesn't cost みや a whole afternoon again.
