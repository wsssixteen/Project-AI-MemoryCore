# Agentic ticket-workflow assessment — 2026-09-06 (DE step 7.5, five axes; one concrete instance per claim)

> Session: weekend system upgrade (P0 turn-ledger + Rules 6/13/14 + feature audit run 1 + plan §9 builds). No etanah ticket was worked; A4 is assessed from what the new tooling changes for the next ticket. Proposals logged via `core/slips.js --type proposal` (ids in `system/slips.jsonl`). Q9 (fold these files into one rolling assessment) is still pending — this file is the last of the loose series if Q9 lands next.

| Axis | Assessment (concrete instance) | Forward idea (eval case) |
|---|---|---|
| **A1 agentic system** | Rule 13 closed the "born blind" hole: forge refused `--goal "fires on every Stop"` (F3c) and a README without `goal:` (birth-gate F4b). But the eval battery had not run since 2026-08-16 and hid 5 red evals — rot is invisible without cadence. | **Battery at DE 12.5 as a gate row**: DE cannot close if `system/telemetry/eval-battery.jsonl` last row is >7 d old. Eval: fixture row dated 8 d ago → C-row blocks; today → passes. |
| **A2 quest workflow** | Every turn row today said `qa: ADHOC-PT-2026-3 · hold` because the stamp copied the top block. Fixed (named → active → null), but the fix landed after the last prompt, so live proof is next session. Wrong-fix ledger + Phase-2 upgrade step exist but have zero real rows. | **First real wrong-fix row from QA-276436** (the REFUTED stashed fix): `lib/wrong-fix.js add QA-276436 …` at next touch; eval = the Phase-2 upgrade table shows it and archive refuses until ruled. |
| **A3 debugging efficiency + accuracy** | The Bash heredoc silently ate backslashes three times (de-close-gate regexes corrupted twice, eval fixture once) — 6 extra turns. The fix that worked every time: write the patch with the Write tool and run the file. | **Rule in `.claude/cost-efficiency.md`**: never pass regex/backslash-bearing source through a Bash heredoc; write a patch file. Eval: `hook-syntax-check` count of `\r?` / `\/g` corruptions in the next 30 d = 0. |
| **A4 etanah issue-solving** | Nothing solved today; the tools that will matter: `wrong-fix.js` for refuted fixes, `system-audit` for where time goes, goal-lens for gate yield. The 23 open blocks could not be reconciled with Redmine (VPN). | **Office-session opener**: reconcile → run the 9b upgrade table on every Redmine-Closed quest; eval = at least one `knowledge:` verdict lands in etanah-knowledge from a real wrong-fix. |
| **A5 sweep / file sweep** | `lib/housekeeping.js` dry-run listed 16 files with retention verbs; the goal drafts touched 52 READMEs in one pass (additive keys only). OneDrive lock ("Device or resource busy") hit one README during a grep. | **Housekeeping real run at this DE** (first ever) then compare `git status` — nothing keep-class touched. Eval: E5 rotation fixture (hook-fires >8 MB → `-YYYY-MM` file, 30 d counts unchanged). |
