# Improvement sweep — 2026-09-09 (PymTime day, no etanah quest touched)

| Axis | Assessment | Instance | Proposal |
|---|---|---|---|
| A1 agentic system | Corrections came in fours before I stopped adding and started proving | icon saga: image → shared ico → re-pointed path → cached address; the 2-line "restore old path" probe settled it | prove-then-change: after 2 corrections on one symptom, next move must be a probe, not a change (eval: turn-ledger refute→wrong-fix advisory fires on the 2nd correction) |
| A2 quest workflow | ⏭ no quest touched; Redmine board shows 2 new tickets (#278930, #278909) without local blocks | boot board | none new; existing `redmine-sync` covers it |
| A3 debugging | A test asserted the bug as intended behaviour and I read it as coverage | `_selftest.js` S10 asserted the duplicate upload | logged: coverage-claim check + shared-asset consumer guard |
| A4 etanah issue-solving | ⏭ no etanah work today | — | — |
| A5 sweep | Personal-string sweep of the bundle found 2 hits after I had already said "no personal data" | reports.js comment + placeholder | build-portable prints the sweep result itself (eval: a bundle containing the owner's name fails the build) |
